// عقد الصوت — **الثوابت مكتوبة مرة واحدة، وكل مسار يُختبَر عليها.**
//
// **لماذا وُجد هذا الملف:** أُصلح عقد #35 في **المسار المباشر** ثم أُنشئ **مسار
// ثانٍ** (محوّل المضيف) لا يعرفه، فعاد العطب نفسه من باب جديد: بعد الكتم صارت
// العجلة لأعلى **تسلّق المستوى صامتاً** («مكتوم 100») ولا تفكّ الكتم.
// **الدرس: عقد محفوظ في نسخة واحدة ليس عقداً.** فصار مكتوباً هنا، ويُشغَّل على
// كل مسار: المباشر، ومحوّل يوتيوب، وكل محوّل قادم (تويتش · كِك).
//
// ⚠️ **شرط بنيوي (قرار المالك): محوّل لا يجتاز العقد لا يُسجَّل في السجلّ.**
// يحرسه `tools/test-volume-contract.js`: يعدّ كل `hostAdapters.set(...)` في
// `content.js` ويشترط لكلٍّ **نموذج مضيف مُعايَراً بالقياس** ثم يُجري العقد
// عليه. محوّل جديد بلا نموذج ⇒ **المجموعة حمراء**، فلا يُكتشف العطب في تويتش
// وكِك واحداً واحداً بل يُمنع قبل التسجيل.
//
// **واجهة «المسار»** التي يستهلكها العقد:
//   { name, step, eps, mid, make(initial) → { get(), up(), down(), badge() } }
//   - `get()`   ⇒ { muted, level }   الحالة كما **يراها المستخدم** بعد استقرارها
//   - `up()` / `down()`              خطوة واحدة، كما لو دار المستخدم العجلة مرة
//   - `badge()`                      آخر نصّ عُرض في الشارة (أو null)
//   - `step`                         خطوة **المسار** (المباشر 0.04 · يوتيوب 5)

// **ثلاث حالات لكل ثابت، لا اثنتان** (قرار المالك 2026-07-31):
//   `ok: true`                  ⇒ اجتاز
//   `ok: false`                 ⇒ مكسور
//   `na: true, reason: "…"`     ⇒ **غير منطبق على هذا المضيف، والسبب معلن**
// الحالة الثالثة ليست ثابتاً مكسوراً ولا ناجحاً: **الفجوة تُعلَن في العقد نفسه
// لا في ملف جانبي**، والمجموعة تبقى خضراء. وكل محوّل قادم **يعلنها أو يجتازها**.
//
// **قدرات المضيف** التي تُصرّح بها المسارات:
//   `mutesByZeroing`  — الكتم عنده **هو المستوى على صفر** (تويتش)، فالمستوى
//                       الظاهر يضيع والكامن يختفي.
//   `latentReadable`  — هل نستطيع قراءة المستوى الكامن وهو مكتوم؟
const INVARIANTS = [
  {
    id: "ع1",
    title: "خطوة لأعلى وهو مكتوم ⇒ يُفكّ الكتم **وتُطبَّق خطوة واحدة** من المستوى الحالي",
    why: "لا يتسلّق المستوى وهو صامت، ولا تُهدر الضغطة. وهذا **عين عطب #35** — " +
         "أول ضغطة رفع بعد الكتم كانت لا ترفع شيئاً، ثم عاد من باب المحوّل بصورة " +
         "أسوأ: ترفع ولا تُسمع.",
    run(path) {
      const h = path.make({ muted: true, level: path.mid });
      const before = h.get();
      h.up();
      const after = h.get();
      const unmuted = after.muted === false;
      const oneStep = Math.abs(after.level - (before.level + path.step)) <= path.eps;
      return {
        ok: unmuted && oneStep,
        detail: `${before.level} ⇒ ${after.level} · muted ${after.muted}` +
                `${unmuted ? "" : "  ← لم يُفكّ الكتم"}${oneStep ? "" : "  ← ليست خطوة واحدة"}`
      };
    }
  },
  {
    id: "ع2",
    title: "خطوة لأسفل وهو مكتوم ⇒ **يبقى مكتوماً** والمستوى ينزل فعلاً",
    why: "المستخدم لم يطلب صوتاً، فلا يُفكّ كتمه. والنزول يقع على **نموذج المضيف** " +
         "لا على قيمة نكتبها نحن ويمحوها هو.\n" +
         "**وعلى مضيف يكتم بتصفير المستوى** (قرار المالك 2026-07-31): لا مستوى " +
         "ظاهر يُخفَض، **فالعملية لا تُنفَّذ ولا تسقط إلى الكتابة المباشرة**. " +
         "السقوط هنا **ضرر لا احتياط**: يكتب قيمة يمحوها المضيف فتتحرّك الشارة " +
         "ولا يتغيّر شيء — وهو عين ما بُني #60 ضدّه. **والفكّ غير المطلوب مرفوض**.",
    run(path) {
      const h = path.make({ muted: true, level: path.mid });
      const before = h.get();
      const rawBefore = h.raw ? h.raw() : null;
      h.down();
      const after = h.get();
      const rawAfter = h.raw ? h.raw() : null;
      const stillMuted = after.muted === true;

      if (path.mutesByZeroing) {
        // المطلوب هنا: **لا شيء يقع**. لا فكّ كتم ولا كتابة مباشرة تتسلّل.
        const noFallbackWrite = rawBefore === null || rawAfter === null ||
          Math.abs(rawAfter - rawBefore) < 1e-9;
        if (!stillMuted) {
          return { ok: false, detail: `انفكّ الكتم وهو غير مطلوب (${before.level} ⇒ ${after.level})` };
        }
        if (!noFallbackWrite) {
          return { ok: false, detail: `تسلّلت كتابة مباشرة: video.volume ${rawBefore} ⇒ ${rawAfter}` };
        }
        return { na: true, ok: true,
          reason: "المضيف يكتم بتصفير المستوى، فلا مستوى ظاهر يُخفَض — " +
                  "والعملية لا تُنفَّذ ولا تسقط إلى الكتابة المباشرة (مُتحقَّق منهما)" };
      }

      const dropped = after.level < before.level - path.eps;
      return {
        ok: stillMuted && dropped,
        detail: `${before.level} ⇒ ${after.level} · muted ${after.muted}` +
                `${stillMuted ? "" : "  ← انفكّ الكتم"}${dropped ? "" : "  ← لم ينزل"}`
      };
    }
  },
  {
    id: "ع3",
    title: "الشارة تعرض الحالة **بعد** العملية دائماً",
    why: "تعرض ما سيسمعه المستخدم لا ما طلبناه. وهذا ما جعل الانحدار مرئياً: " +
         "«مكتوم 100» كانت **صادقة** — هي التي كشفت أن المستوى يتسلّق صامتاً.",
    run(path) {
      const out = [];
      for (const [label, init, op] of [
        ["رفع على مكتوم", { muted: true, level: path.mid }, "up"],
        ["خفض على مكتوم", { muted: true, level: path.mid }, "down"],
        ["رفع على غير مكتوم", { muted: false, level: path.mid }, "up"]
      ]) {
        const h = path.make(init);
        h[op]();
        const st = h.get();
        const txt = h.badge();
        if (txt === null || txt === undefined) { out.push(`${label}: لا شارة`); continue; }
        const raw = String(txt);
        const markOk = /مكتوم/.test(raw) === st.muted;
        const hasNumber = /[0-9]/.test(raw);
        // **الشارة تعرض ما سيسمعه المستخدم عند الفكّ، لا ما يقرأه العنصر وهو
        // مكتوم** (قرار المالك 2026-07-31). فحيث يكتم المضيف بتصفير المستوى:
        // الكامن مقروء ⇒ «مكتوم ‹الكامن›» · غير مقروء ⇒ **العلامة وحدها بلا رقم**.
        // و«مكتوم 0» **ممنوعة**: رقم نعلم أنه كاذب أسوأ من غياب الرقم.
        if (st.muted && path.mutesByZeroing && !path.latentReadable) {
          if (!markOk || hasNumber) {
            out.push(`${label}: «${raw}» — المطلوب العلامة وحدها بلا رقم`);
          }
          continue;
        }
        const shownLevel = Number(raw.replace(/[^0-9.]/g, ""));
        const expected = path.badgeLevel(st.level);
        const levelOk = Math.abs(shownLevel - expected) <= 1;
        if (!markOk || !levelOk) {
          out.push(`${label}: «${raw}» بينما الحالة ${st.muted ? "مكتوم " : ""}${expected}`);
        }
      }
      return { ok: out.length === 0, detail: out.length ? out.join(" · ") : "الثلاث حالات مطابقة" };
    }
  }
];

// ── حدود معلنة: مضيفون **لا محوّل لهم بقرار مسنود بالقياس** ────────────────
// **ليست فجوة منسيّة ولا نيّة مؤجَّلة**: قرارٌ بأرقامه، مكتوب **في العقد نفسه**
// كي يجد من يفتح الملف بعد سنة **لماذا لا محوّل** فلا يعيد الطريق كلّه.
// ويحرسه `tools/test-volume-contract.js`: **تسجيل محوّل لمضيف معلن حدُّه يُحمّر
// المجموعة** — فلا يُكتب محوّل دون إسقاط الحدّ صراحةً وبقياس جديد.
const DECLARED_LIMITS = [
  {
    host: "kick.com",
    date: "2026-07-31",
    verdict: "**لا محوّل. والمسار المباشر يخدمه**، فكتابتنا تنجو عنده في السكون " +
             "كإخوته — **وكِك داخل عائلة #60 لا خارجها**.",
    // الروافع الأربع، كلها مقيسة ومغلقة — لا واحدة منها مفترضة
    levers: [
      ["المنزلق لا يُقاد",
       "يُعرض بـ`:hover` **حقيقيّ** فمقاسه **0×0** دائماً · `تركيز=لا` في **19 محاولة** · " +
       "جامد على الأسهم و`Home`/`End`/`PageUp` · وعلى **8 عجلات** على المنزلق وأبيه وجدّه والفيديو"],
      ["لا نسخة مشغّل معروضة",
       "بحث بالسلوك (≥2 من `setVolume`/`getVolume`/`setMuted`/`getMuted`/`isMuted`) على " +
       "`window` وعلى عنصر الفيديو ⇒ **لا شيء**. IVS يشغّله لكنه **مغلق علينا**"],
      ["لا مفتاح تخزين",
       "`amazon_ivs_*` و`kick:player_device_id:v1` ⇒ **ولا مفتاح صوت بينها**"],
      ["والكتابة المباشرة تنجو في السكون",
       "30% صمدت **أربع ثوانٍ** (`0→100→300→600→1000→2000→4000ms`)، **مؤكَّد مرتين**"]
    ],
    effect: "**ضياع المستوى في دورة الكتم**: قبل الكتم 30% · كتم عند 2400ms · " +
            "فكّ عند 6400ms · فعاد **38%** لا 30% ⇒ **كِك استعاد نموذجه هو**.",
    note: "⚠️ **والحدّ بنيويّ لا برمجيّ**: حالة `:hover` تتبع موضع المؤشّر الفعليّ " +
          "**ولا يصنعها أي حدث مُرسَل**، فالتمرير المُصطنَع **لن ينجح أبداً مهما حُسّن**."
  }
];

function runContract(path) {
  return INVARIANTS.map((inv) => {
    let r;
    try { r = inv.run(path); }
    catch (err) { r = { ok: false, detail: "استثناء: " + String(err && err.message || err).slice(0, 80) }; }
    return { id: inv.id, title: inv.title, ...r };
  });
}

module.exports = { INVARIANTS, runContract, DECLARED_LIMITS };
