# HANDOFF — تعليمات استئناف العمل

> هذا الملف مكتوب لمن يفتح المشروع في جلسة جديدة **بلا أي معرفة سابقة** بما جرى.
> اقرأه كاملاً قبل أن تكتب سطراً واحداً. المرجع التفصيلي للأخطاء هو `AUDIT.md`.
>
> **وكلُّ سطرٍ فيه يُقرأ في كل جلسة.**
> **وما لا يُقرأ في كل جلسة خرج إلى مرجعه** (#81، قرار المالك: القسمةُ
> **بالاستعمال لا بالموضوع — فالمهمُّ الذي لا يُقرأ يزاحم المهمَّ الذي يُقرأ**).

| المرجع | **متى يُقصد** | كان |
|---|---|---|
| [`docs/DECISIONS.md`](docs/DECISIONS.md) | عند بندٍ يذكر رقم قرار · وحين تكتب رِكازاً (شواهد 26) | §4 |
| [`docs/PLATFORMS.md`](docs/PLATFORMS.md) | قبل تشغيل منصّةٍ يدوية، أو حين يمسّ عملُك ما تحرسه | §8 · §9 · §10 · §10ب–د · §13 · §14 · §15 · §16 |
| [`docs/HISTORY.md`](docs/HISTORY.md) | «لماذا صار هذا هكذا؟» · وكومِتات البنود الماضية | §0ب · §1 · §2 · §2ب · §3 · §12 · §17 |
| [`docs/REFERENCE.md`](docs/REFERENCE.md) | بنية المشروع واختباراته · الأرقام المقيسة · git | §5 · §7 · §11 |

⚠️ **ولا يُضاف قسمٌ إلى هذا الملفّ إلا بعد سؤال «أيُقرأ في كل جلسة؟»** —
**و`tools/test-handoff-split.js` يُحمّر على قسمٍ ليس في قائمته**، فالنموّ
بالتراكم (2724 سطراً) لا يعود بلا قرار.

---

## 0. أين تستأنف — اقرأه أولاً

### ✅ السطر الأوّل: **`main` على `2.15.0`** — ⛔ **ولا نسخة حتى تُسحب الثلاثةُ والعشرون**

⚠️ **ولا تُصدَّق هذي السطور: يُتحقَّق بـ`git log origin/main..HEAD` (صفر) و
`git status`** — **والشجرةُ النظيفة لا تعني المدفوعة** (قرار 40).
⛔ **والعددان (البنود · التأكيدات) لا يُكتبان هنا** — يُطبعان من
`node tools/audit-status.js` و`node tools/run-tests.js` وحدهما. **وقد تخلّف رقمٌ
مكتوبٌ هنا مرّةً 2026-08-04 فحُذف ولم يُصحَّح** (قرار 34: موضعٌ ثانٍ للحقيقة
يتخلّف بالبناء).

### ⛔ البوّابةُ **ثمانية** لا سبعة — والثلاثةُ والعشرون «عند الطلب» بمُطلِقها

**الثمانية في «الحدّ الأدنى» أدناه، ويحرسها `tools/test-gate-members.js`.**
⛔ **وما عداها — 23 رِكازاً — «يُشغَّل عند الطلب» بسببٍ مكتوب: تحتاج مضيفاً حيّاً.**
⇒ ⛔ **ولا تُرفع نسخةٌ حتى تُسحب الثلاثةُ والعشرون ويُنسب أحمرُها** (سابقٌ · منّا ·
تباينُ مضيف · حدٌّ مُعلَن) — **مكتوبٌ في بروتوكول النسخة نفسِه في `CLAUDE.md`**،
فما لا يكون في البروتوكول لا يقع.
⚠️ **وهذا أوّلُ تطبيقٍ للشرط، فسيُختبر الشرطُ نفسُه فيه.**

### 🎯 المفتوح — بمُطلِقاته

| البند | مُطلِقُه / حالُه |
|---|---|
| **#93** | ⏳ **بقي فيه شطران:** نسخةُ [٣] المحلّية في `bench-options-page` (تُحذف وتستورد المشترك) · و`wanted` التي لا تُرجَع |
| **#100** | ✅ **مُصلَح** (13 موضعاً) — **وينتظر تحقّقاً ميدانياً** |
| **#102** | مسارٌ احتياطيّ `\|\| document.body` يمكن أن يكون `null` |
| **#104** | رِكازٌ يُعلن بطلان قياسه ثمّ يحكم به فيتّهم #91 |
| **#105** | تصنيفُ رمز الفشل الثلاثيّ · **مُطلِقُه: أوّلُ سحبٍ للـ23** — ⭐ **وقد وقع مرّتين اليوم** |
| **#75 · #79 · #80 · #68ب · #83 الباقية** | مؤجَّلةٌ بقرارٍ أو تنتظر مُطلِقاً لم يقع |
| **`S11` · `S12`** | رصدُ عَرَض · و**`S12`: كودٌ واحد بنتيجتين، ومُطلِقُه ثالثةُ تشغيلة** ⛔ **ولا يُغلق بـ«الآن يعمل»** |
| **`S2`–`S5`** | ⏳ **تنتظر متصفّحك وحدها** — وخطواتُها **`م23`–`م26`** مكتوبةٌ في [`tools/CHECKLIST.md`](tools/CHECKLIST.md) بسندِ كلٍّ وسطرِ «المقيس» فوقها · **و`م27` لـ#103** |

### ⭐ ومكسبُ 2026-08-04 في سطر

**ثمانيةُ عيوبٍ في أدواتنا أُمسكت قبل النشر** · **ومحوّلُ الصوت قِيس فنجا**
(`ع55/ن55` إلى 8000ms وعبر دورة كتم) · **وعقدُ الصوت كان محروساً وغيرَ مُشغَّل —
والجردُ هو ما كشفه**. ⇒ **ولا عطبَ نائماً بقي إلا #103، وقد أُصلح.**

### ✅ ما أُغلق في جلسة 2026-08-03/04 — **أحدَ عشرَ بنداً**

| البند | بماذا أُغلق |
|---|---|
| **#94 · #96** | «فيديوٌ يملك أدواته» — الزرّ لا يُرسم في نطاقٍ أدواتُه مخفيّة، والشريط من **نطاق الفيديو** لا المستند. **وتحقّق المالك** |
| **#91** | عنوانُ ملء الشاشة عنصرٌ آخر (`.ytp-fullscreen-metadata`) — والمفتاح صار يفي. **وتحقّق المالك** |
| **#92** | ❌ **«غير ممكن» بسببٍ مقيس**: اللافتة فقاعةُ المتصفّح لا عنصرٌ في المستند — **ولا مفتاح** |
| **#86** | الامتناع العالق زال بالتوفيق من `e.buttons`. **وتحقّق المالك** ⇒ **ورُفعت النسخة `2.15.0`** |
| **#97** | العدُّ نفى اللاحتميّة (تسعُ عيّنات) — **والعطب كان في السؤال لا في الجواب، والمنتج لم يُلمس بحرف** |
| **#98** | 11 من 11 متطابقة — **والمقدّمة سقطت قبل النتيجة**: `<html>` في المسارين معاً |
| **#93** | الشطر الأوّل: أربعةُ حدودٍ عددية صارت محروسة · والثاني: **أربعُ عللٍ لا واحدة** |
| **#99** | 70 حارسَ وعدٍ · **2 حارسَ ثابتٍ داخليّ** — وحارسٌ فوقهم يُحمّر على الغياب |
| **#81** | `HANDOFF` من **2750 إلى 431** سطراً · أربعةُ مراجع · وحارسٌ ثلاثيّ · **والضائع صفر** |
| **`S10`** | ✅ **قِيس**: في التضمين **الترجمة والأتمتة وClean Player ميتة، والحاوية والمربّعات تعمل** |

### ✅ وما أُغلق 2026-08-04 (الجلسة الثالثة والعشرون) — **`S9` وقياسُ الجودة**

| | بماذا |
|---|---|
| **`S9`** | ✅ **مغلق فحصاً — لا عطب. القسم الخامس عشر.** **الحاجز يسبق معالجَ المضيف في الأوسط والأيمن معاً**، **والأيمن مُثبَتٌ بأثر المضيف نفسِه**: قائمةُ يوتيوب تظهر بلا ربطنا **ولا تظهر معه**. **وتحقّقُ المالك غير مطلوب: القياس آليّ** |
| **والجودة في التضمين** | ✅ **أُعيدت بعيّنتين — القسم السادس عشر.** `480 ⇒ 480` **لم تتحرّك في ستّين ثانية**، **والمشغّل يعرض `hd720` ويُعلن `large`**. ⚠️ **ولا عطبٌ أُعلن ولا سلامة — الحكم للمالك** |

### ✅ وقرارُ المالك في الجودة صدر — **لا تُبنى التغطية، ويُصحَّح الوعد**

**#68ب يبقى مؤجَّلاً كما هو.** **وما لا يُبنى يُقال:** الأربعُ الميتة في التضمين
(الترجمة · أتمتة اللغة · Clean Player · الجودة) **صارت وسومُها تقول أين تعمل** —
`data-vz-embed` في `options.html` و`popup.html`، **والتوثيق معها**
(`CLAUDE.md` كان يذكر Clean Player وحده منذ #68، والبوب-أب كان يقول **«على جميع
المواقع»**). ويحرسها `tools/test-embed-promise.js`.

### 🎯 التالي — ⛔ **حالةٌ مُعلَنة: لا بندَ جاهزاً للبدء، إلا واحداً بقرارك**

> ⚠️ **تُقرأ هذي الحالُ ولا تُستنتَج** (قرار المالك 2026-08-04): **من قرأ قائمةً
> بعشرة مفتوحين ظنّ أن ثمّة عملاً ينتظر** — **والمفتوحُ ليس المستحقّ.**

| المفتوح | حالُه — **ولكلٍّ سببٌ لا تقدير** |
|---|---|
| `S2` · `S3` · `S4` · `S5` | ⏳ **ينتظر متصفّحك** — **وقد قِيس منها آلياً ما يبلغه الرِكاز 2026-08-04**، والباقي مكتوبٌ خطواتٍ في [`tools/CHECKLIST.md`](tools/CHECKLIST.md) (`م23`–`م26`) **بسندِ كلٍّ وسطرِ «المقيس» فوقها** |
| #68ب · #79 · #80 | 🔒 **مؤجَّلٌ بقرارٍ صريح منك** — ولا يُفتح لأن مانعاً زال (قرار 75) |
| #75 · `S11` | 🔔 **ينتظر مُطلِقاً لم يقع** — مستهلكٌ ثالث للمفتاح · ورصدُ عَرَض |
| **#93** | ⚠️ **الوحيدُ الذي وقع مُطلِقُه وينتظر قرارك** — أُجيب سؤالُه الجذر، **وبقي فيه عطبٌ واحد مقيسٌ اليوم**: ⇊ |

⛔ **وهو أخطرُ ما في هذي الحالة، فيُقرأ قبل تصديق أي أخضر:**
**قسم [٣] في `tools/bench-options-page.mjs` أخضرُ عن عمى في ضوابطه الـ46 كلِّها**
— تعبيرُه يرمي `ReferenceError: a is not defined`، **وCDP يُرجع `{}` لا
`undefined`** فيمرّ صادقاً بحقولٍ غائبة، **وغيابُها يُقرأ «لا عيب»**.
**والشاهد السلوكيّ: أُعيد عطبُ #89 بنصّه فبقي الفحص 18/18 أخضر.**
⇒ **والتفصيل في [`tools/KNOWN-DEFECTS.md`](tools/KNOWN-DEFECTS.md)** — **ولم
يُصلَح، بانتظار قرارك.** ⚠️ **وباقي أقسامه ([١] · [٢] · [٤] · [٦] · [٧]) صالحة**،
فلا يُهجر الفحص ولا يُصدَّق كلُّه.

### ⭐ وثلاثةٌ من جلسة `S9` تُقرأ قبل أي عمل

1. ⭐ **العطب كان في الأداة لا في المنتج، وكان سيُنتج نجاحاً لم يقع:** حكم «قُتل
   الحدث» مشدوداً إلى `win-cap:click` **و`click` لا يقع على أوسطَ ولا أيمن** ⇒
   **الحالات الأربع كانت ستُطبع «قُتل الحدث» ومنها اثنتان بلا إضافةٍ محمَّلة.**
   ⇒ **والإثباتُ الكاذب أخطر من الصفر الكاذب** — وهذا ثاني موضعٍ يقع فيه.
2. ⭐ **ولا يُكتفى بالامتناع عن نشر صفر: يُبحث عن أثرٍ يصلح شاهداً أوّلاً**
   (قرار المالك). قيل «الأوسط قد لا يكون له أثرٌ مرئيّ» — **فوُجد له**: تبويبٌ
   يُفتح بنقرةٍ وسطى على رابط، `2 ⇒ 3` **محلياً وعلى المضيف نفسِه**.
   **والتمرير التلقائيّ جُرّب فلم يقع على كروم/ماك** ⇒ **يُعلَن مُجرَّباً ساقطاً
   لا مُهمَلاً** — «جرّبتُ كذا فسقط» لا «لا أثر».
3. ⭐ **وعيّنتان متطابقتان لا تُعلَنان عطباً ولا سلامة** (قرار المالك): تطابقُهما
   يقول «لم تتحرّك» **ولا يقول لماذا** — **فالسببُ يُقاس على حدة**، وقد قِيس:
   ناقلُنا غائبٌ من الإطار، **وعلّتان لا واحدة، فقِيس الأصلان لا أصلٌ واحد**
   (قرار 42) لئلا يُصلَح سببٌ فلا يتغيّر شيء.

### 🎯 والمفتوح الباقي — **بمُطلِقاتِه، ولا يُفتح شيءٌ لأن مانعاً زال** (قرار 75)

| البند | **مُطلِقُه** |
|---|---|
| **#79** | وقوعُ مُطلِقه القائم — **لا زوالُ مانع** |
| **#80** | **صفحةٌ ثانية مكسورة يمرّ عليها فحصُ التحميل** (تسرّبت `persistTiming` مرّةً) |
| **#83 الباقية** | ما بقي من بنده |
| **#68ب** | **بعد `S10`** — وقد قِيس، **فمُطلِقُه وقع**: ثلاثُ ميزاتٍ ميتة في التضمين |
| **#75** | مستهلكٌ ثالث للمفتاح أو حاجةٌ تُوجب تغييره |
| **`S2`–`S5`** | ⚠️ **تنتظر متصفّح المالك — ولا تُبدأ بلا خطواتٍ مكتوبة له في [`tools/CHECKLIST.md`](tools/CHECKLIST.md)** |
| **`S11`** | رصدُ عَرَض |
| ~~**`S9`**~~ | ✅ **أُغلق 2026-08-04 فحصاً — لا عطب** (القسم الخامس عشر) |
| ~~**#81**~~ | ✅ **مُنفَّذ `2e2bce3`** — وبقي صفُّه في `AUDIT` يقول «مؤجَّل» حتى صُحّح 2026-08-04: **فجوةُ تسجيلٍ لا تنفيذ** |

### ⭐ وثلاثةٌ من هذي الجلسة تُقرأ قبل أي عمل

1. **الشكُّ أدّى غرضه**: `S10` **سمّى ثلاث ميزاتٍ تموت صامتة** — **وهذا ما تصلح
   له الشكوك، لا أن تُغلق بقرار**.
2. **والحارس أمسك ملفَّنا نحن في يومه**، فأُبدل المسارُ الاحتياطيّ بامتناعٍ
   مُعلَن: ⭐ **«لا عالمَ ⇒ لا قياس» أصحّ من قياسٍ في عالمٍ خطأ**.
3. ⭐ **ودرسُ `const`-في-`vm` أُمسك قبل أن يُنشر رقم — للمرّة الأولى بعد ثلاث**:
   `KNOWN_PLAYER_WRAPPER_SELECTOR` أعطى `-1` **لأن المِجَسّ يشترط النوع قبل
   القراءة**. ⇒ **فالقاعدة صارت تعمل قبل الوقوع لا بعده.**

⚠️ **وواقعةٌ رابعة سجّلها المالك على نفسه (2026-08-04):** وصف `S10` **من ذاكرته
لا من ملفّه** («أتُسرَّب الإعدادات إلى `iframe`؟») — **وسؤالُ خصوصيةٍ وسؤالُ ميزةٍ
تموت صامتة ليسا شيئاً واحداً**. ⇒ **وقياسُ ما أُملي كان سيُنتج جواباً صحيحاً
لسؤالٍ لا أحد سأله** (قرار 16: يُقاس المكتوب ويُبلَّغ الفرق).

### 🎯 (المحفوظ) — بترتيب المالك (2026-08-04)

**#86** — وهو **آخرُ ما يمنع النسخة** · **ثمّ النسخة دفعةً واحدة** · **ثمّ حالة
المفتوح بتقسيمها الثلاثيّ** (قياسي · قرار المالك · متصفّح المالك).
⚠️ **و#97 سُجّل اليوم**: قسم #70 في `bench-overlay-layer --youtube` **غير حتميّ**
— **كودٌ واحد أعطى نتيجتين** — **وهو حارسٌ نحكم به على الميزات، فأخضرُه بالحظّ
يُفرغ الأحمر من معناه**. ⛔ **ولا يُقرأ أحمرُ ذلك القسم انحداراً حتى يُكرَّر.**

### ⚠️ سطران يخصّان المالك — يُقرآن قبل كتابة أي خطوة له

1. **يوتيوب عنده بالعربية فالمشغّل معكوس** ⇒ **المواضع تُكتب بالوظيفة لا بالجهة**.
2. **وقائمته في [`tools/CHECKLIST.md`](tools/CHECKLIST.md) لا في `HANDOFF`**،
   **بأسنادها الثلاثة**: 📐 مقيسة · 👁 مُشاهَدة · 📖 من قراءة كودٍ لم تُشاهَد —
   **والثالثة تُقرأ سؤالاً لا متوقَّعاً** (قرار 52).

### حالة المستودع

| | |
|---|---|
| **`main`** | ✅ **مدفوعةٌ بالكامل عند نهاية جلسة 2026-08-04** — **ولا فرع نشط**. ⚠️ **ولا تُصدَّق هذي السطر: يُتحقَّق بـ`git log origin/main..HEAD` (صفر) و`git status`** — **والشجرةُ النظيفة لا تعني المدفوعة** (والتجزئة تُشتقّ ولا تُكتب هنا، قرار 40) |
| **النسخة** | **`2.15.0`** — رُفعت 2026-08-04 **دفعةً واحدة بعد إغلاق #86** (minor: ميزاتٌ وإصلاحات). **والمغلق منذ `2.14.0`: #85 · #86 · #87 · #88 · #89 · #90 · #91 · #92 · #94 · #95 · #96** |
| **الاختبارات** | ⛔ **ولا هذا** — يُطبع من `node tools/run-tests.js` وحده |
| **البنود** | ⛔ **لا يُكتب هنا عدد** — يُطبع من `node tools/audit-status.js` وحده. **وقد تخلّف رقمٌ مكتوبٌ هنا مرّةً 2026-08-04، فحُذف ولم يُصحَّح: موضعٌ ثانٍ للحقيقة يتخلّف بالبناء** (قرار 34) |
| **فحص التحميل** | `node tools/bench-options-page.mjs` **شرطُ قبولٍ لا تحسين** (قرار 39) — ~~9 من 9 بالشاهدين~~ ⛔ **مسحوب (قرار 21)** · **والمقيس على متصفّحٍ نظيف 2026-08-03: 12 من 12 بالشاهدين** |

### وقبل أي عمل — الحدّ الأدنى: **ثمانيةٌ** (قرار المالك 2026-08-04)

```bash
node tools/run-tests.js --quiet        # صفر فشل
node tools/audit-status.js             # يخرج بصفر — ولا صفّ بلا علامة
node tools/bench-options-page.mjs      # الصفحة تحيا وتتفاعل
node tools/bench-overlay-layer.mjs     # والطبقة تعمل كما يستعملها المستخدم
node tools/bench-eval-contract.mjs     # وأداتُنا تقول حين ترمي  (#93)
node tools/bench-s69-guards.mjs        # ومسارُ فشل الحفظ يُرجع الضابط (#69 · #103)
node tools/repro-58-fullscreen.mjs     # واختيارُ الحاوية على البنيات العشر (#58 · #17)
node tools/lint-names.mjs              # ولا اسمَ يُستعمل حيث لا يُحلّ (#77 · #82 · #100 · #101)
```

⭐ **ولماذا هذه الثلاثة بعينها ولا رابعَ معها** (والمعيار هو الاختيار، لا «الأهمّ»):
**محليّةٌ بلا شبكة · حتميّةٌ لا تتباين بالمضيف · وتحرس عقوداً مسّها عملُنا.**
⛔ **وما عداها يحتاج مضيفاً حيّاً**، فتشغيلُه في كل كومِت **يُدخل الشبكةَ والحجبَ
في بوّابة الكومِت، وأحمرُه يصير ضجيجاً فيُقرأ إذناً بالتخطّي** (تحذير #97).

⚠️ **والثمانية معاً هي «التحقّق» في قرار 32 بنصّه الثالث — لا `run-tests` وحدها،
ولا يُستثنى منها شيء بحكمٍ على ما لُمس.** ⛔ **وقد جُرّب الاستثناء مرّةً فسقط:**
«لم أمسّ `options.*` فالفحص غير لازم» — والصفحة كانت تحمل `ReferenceError`
وقتها، **كشفه المالك من `chrome://extensions` لا نحن**.

### ⛔ وما ليس في السبعة: **«يُشغَّل عند الطلب» بسببٍ مكتوب — ومُطلِقُه رفعُ النسخة**

**السببُ المكتوب لثلاثةٍ وعشرين رِكازاً: تحتاج مضيفاً حيّاً.** ⛔ **وهذا سببٌ
لتأجيلها عن الكومِت، لا إذنٌ بألّا تُشغَّل أبداً** — **وقد نامت فيها #103 أسابيع.**
⇒ ⭐ **فالمُطلِق (قرار المالك 2026-08-04): لا تُرفع نسخةٌ حتى تُشغَّل الثلاثةُ
والعشرون ويُنسب أحمرُها.**
**ولماذا رفعُ النسخة هو المُطلِق:** «عند إغلاق كل موجة» لم يعد يقع — لسنا في
موجات. ⇒ **ورفعُ النسخة هو اللحظة التي يصير فيها ما لم يُقس دعوىً عامّة.**

⛔ ~~**و`bench-overlay-layer` أحمرُ اليوم بثلاثة شروط**~~ — **مسحوبٌ 2026-08-04
(قرار 21)، وقد زالت الثلاثة بإغلاق #72** وشُطبت في `tools/KNOWN-DEFECTS.md` منذ
2026-08-03، **وبقي السطرُ هنا يقول عكسها** — وهو عينُ ما يحذّر منه قرار 21:
**رقمٌ ميّت في ملفٍّ ثانٍ يُقرأ حجّةً وقد سقط سنده**.
✅ **والمقيس 2026-08-04 بلا معاملات: 18 من 18 لصفحة الإعدادات · وكلُّ شروط
الطبقة خضراء.** ⚠️ **وقسما #85 و#70 يُعلنان أنهما «لم يُقاسا»** — يحتاجان
`--youtube`، **و«لم أقس» ليست «قِستُ فوجدت صفراً»**.

### ⭐ العميان الأربعة — يُقرأ هذا قبل كتابة أي رِكاز

**العائلة واحدة: الأداة ترى · والمقيس صحيح · والمقيس ليس المنتَج.**

| # | الواقعة | ما قُرئ خطأً |
|---|---|---|
| **١** | «مرئي» = مستطيل غير صفريّ (`S7`) | عنصرٌ بشفافية صفر طُبع «مرئياً 1081×40» |
| **٢** | «متطابق» في حارس المفاتيح (#66) | «39 مفتاحاً متطابقاً» في **كل** الحالات — والحارس أعمى |
| **٣** | `hides` تحوي نموّاً (#70) | إعادة تدفّق قُرئت إخفاءً — **والعلاج تصنيفٌ لا تسميةٌ أدقّ** |
| **٤** | معرّف الإضافة بالتخمين (#77) | التُقطت **إضافة متجر كروم**، فطُبع «صفر استثناء» عن `chrome-error://` |

⇒ **القاعدة الجامعة: كل وصفٍ يُشتقّ من الشرط المقيس ولا يسبقه** — و«مرئي»
و«متطابق» و«يُخفي» و«صفر خطأ» **أربعتها أُطلقت أوسع من شرطها**.
⇒ **وشاهدان قبل أي رقم (قرار 26): موجبٌ يُثبت أن المِجَسّ يرى، وسالبٌ يُثبت أنه
يُحمّر.** ولا يُنشر رقم من رِكاز بلا شاهدين.

### ⭐ وعائلةٌ ثانية اكتملت بثلاث وقائع: **أداةٌ تُثبت عطباً لا وجود له**

**والعميان أعلاه يُخفون عطباً واقعاً؛ وهذي تصنع عطباً لم يقع** — **وهي أخطر في
الوقت**: تُنفق الجلسةَ في مطاردة ما ليس هناك، **وتُفقد الثقةَ في أخضرَ صحيح**.

| # | الواقعة | ما أثبتته زوراً |
|---|---|---|
| **١** | مِجَسّ #70 قاس **«مخفيّ»** فخلط **إخفاءنا بإخفاء المضيف** (يوتيوب يُخفي شريطه بعد ~3 ثوانٍ) | «الميزة لا تعمل» — **وهي تعمل** |
| **٢** | الرِكاز **تخلّف عن تغيير مقصود** (#38أ+ب): أدار العجلة على مربّعٍ **بلا ربط**، فطبع `0 عنصر` | «انحدارٌ في المنتج» — **والصفر سلوكٌ صحيح** |
| **٣** | منصّة #70 حوّمت على **80%** من الارتفاع **فوقع المؤشّر داخل الشريط** — و**#95 يمتنع بحقّ** | «الإخفاء لا يقع» — **وامتناعُه هو الصواب** |
| **٤** | مِجَسّ #98 قاس «أتعلو طبقتُنا؟» بـ`elementFromPoint` — **وهي `pointer-events:none` بالتصميم** | «الطبقة لا تعلو» — **والمتصفّح لا يردّها أصلاً** |

⚠️ **والرابعةُ تستحقّ أكثر من إلحاقها (قرار المالك): هي أوّل مرّةٍ يمنع فيها
الشاهدُ نشرَ رقمٍ عن خاصيّةٍ نحن من وضعناها** — **فالأداة عميت عن تصميمنا نحن لا
عن المضيف**. ⇒ **وحين تقيس ما بنيتَه بيدك، اسأل: أيّ خاصيّةٍ منّا تمنع هذي الآلة
من الرؤية؟** — فالمضيفُ يُدرَس، **وتصميمُنا يُنسى لأنه معلوم**.

⇒ **والجامع: الأداة أنتجت حالاً غير التي تظنّ، ثمّ وصفتها بوصف الحال المقصودة.**
⇒ **والعلاج في الثلاث واحد: تُنتَج الحال ويُتحقَّق منها قبل أن يُقرأ عليها رقم**
(قرار 22)، **ويُسأل سببُنا لا الأثر** (قرار 48 · 72).

### ⭐ وعائلةٌ ثالثة سُمّيت 2026-08-04 بقرار المالك: **غيابُ الإشارة يُقرأ إثباتاً**

**وهي مقلوبُ الصفر الكاذب تماماً:** هناك **يُقرأ الصفرُ نفياً**، وهنا **يُقرأ
النفيُ إثباتاً للطرف الآخر** — والحكم يخرج **واثقَ الصياغة** فيُبنى عليه.

| # | الواقعة | غيابُ ماذا | قُرئ إثباتاً لماذا |
|---|---|---|---|
| **١** | حكم «قُتل الحدث» في `bench-s6-click` (`S9`) | `win-cap:click` — **ولا يقع لزرٍّ أوسط أو أيمن أصلاً** | «حاجزُنا نجح» — **على صفحةٍ بلا إضافةٍ محمَّلة** |
| **٢** | «أيمن سالب ⇒ ✅ لا قائمة» (`S9`) | حقلُ `hostMenu` **لم يُقرأ في السجل أصلاً** | «القائمة غائبة» |
| **٣** | مطابقةُ #92 على «Rescued» | — (وهي أختُها المقلوبة: **مطابقةٌ أوسع من سؤالها**) | «ثمّة عنصر» عن عنوان فيديو |

> ⭐⭐ **وبابٌ رابع سُمّي 2026-08-04 وقلبَ فهمَ العائلة نفسِها — بعد `node --check`
> و`String.replace` و`catch` الفارغ: طريقٌ يفشل ولا يقول، في مُساعدٍ نستعمله في
> كل قياس.** `evalIn` في `tools/ext-harness.mjs` كانت تُرجع القيمة **ولا تقرأ
> `exceptionDetails`** — **و`returnByValue` يُسلسِل كائن `Error` إلى `{}`**،
> كائنٌ فارغٌ **صادقٌ في `if`**. ⇒ **فوعدٌ مرفوض يمرّ نجاحاً (إثباتٌ كاذب)،
> ورميةٌ متزامنة تمرّ عدماً (صفرٌ كاذب)، و`undefined` المشروعة لا تُفرَّق عن
> الرمية.** ⇒ ⭐ **مُساعدٌ واحد يُنتج القطبين معاً، وقد ظننّاهما بابين** —
> **وهذا ما لا يُرى إلا بقياس المُساعد نفسِه، لا بقراءة مُستهلكيه.**
> **وحجمُه مقيس:** 41 موضعَ `Runtime.evaluate` · **17 يبتلع** · و`evalIn` وحدها
> **يستهلكها 17 ملفاً بـ253 نداءً**. ⛔ **والمُثبَت يُفصل عن غيره:** أن 17 موضعاً
> يبتلع **مُثبَت**، وأن أيّاً من الـ253 **مُضلَّلٌ فعلاً غيرُ مُثبَت** —
> **فالابتلاعُ شرطٌ لازم لا كافٍ**، وشاهدُه `contentWorld`: يبتلع **ولا يُضلَّل**
> لأنه يشترط `=== true` فلا تخدعه `{}`.
> ⚠️ **وحدٌّ على الأرقام القديمة:** كلُّ قياسٍ مرّ عبر `evalIn` منذ كُتبت يحمل
> هذا الاحتمال. ⛔ **ولا يُبطَل شيءٌ جملةً** — **لكن أيُّ رقمٍ يُستشهد به لاحقاً
> ويعتمد عليها يُعاد قياسه ولا يُقتبس.**

⇒ ⭐ **القاعدة (قرار المالك 2026-08-04): حكمٌ مبنيٌّ على غياب إشارة لا يُنشر حتى
يُثبَت أن الإشارة تقع حين تجب.** — **وهو قرار 26 مطبَّقاً على الأحكام لا على
الأرقام**: الشاهدان كانا لِـ«أيصدَّق هذا الرقم؟»، وصارا لِـ«أيصدَّق هذا الحكم؟».
⇒ **والإثباتُ الصحيح للحارس: يُشغَّل على الملفّ السابق بنصّه من السجل** —
`git show HEAD:<الملفّ>` — **لا على عطبٍ مشابهٍ يُفتعَل** (قرار المالك، وطُبِّق
في `S9`: أحمر 18/أخضر 21 · وفي الوسوم: أحمر 24/أخضر 29).

### ⭐ وقاعدةٌ ثانية من القياس نفسه: **حالٌ تجتمع فيها علّتان لا تُقاس وحدها**

**يُبحث عن حالٍ تعزل إحداهما** (قرار المالك 2026-08-04). **الواقعة:** جودةُ
التضمين على `youtube-nocookie.com` تسقط عند **بوّابتنا** (`isYouTubeHost()`)
**وناقلِنا** (`__vzQB`) **معاً** — ⇒ **فمن قاسه وحده يقرأ سبباً واحداً، فيُصلح
البوّابة ولا يتغيّر شيء**. و`youtube.com/embed` يُسقط الأولى ويُبقي الثانية،
فقِيست **مستقلّةً** بدل أن تُستنتج من قراءة المانيفست. **وهي قرار 42 (عَرَضٌ
واحد لا يدلّ على سببٍ واحد) مطبَّقاً على اختيار الحال المقيسة لا على التشخيص.**

### ⭐ وما تُنتجه الشكوك حين تُفحص — **وعدٌ صادق، لا صفر** (قرار المالك)

**`S10` و`S9` لم يُنتجا إصلاحاً في الكود، وأنتجا وعداً صادقاً**: أربعُ ميزاتٍ
كانت تَعِد بما لا تفعل في التضمين **صارت تقول أين تعمل** (`data-vz-embed`،
ويحرسها `tools/test-embed-promise.js`). ⇒ **وشكٌّ يُغلق بلا تغيير كودٍ ليس شكّاً
ضائعاً** — **والقياس الذي لا يُنتج إصلاحاً قد يُنتج صدقاً**، وهو ما صُرف #66
و#67 في تحصيله.

### ⭐ وخطوةُ «قِس مدى الإصلاح قبل أن تبني عليه» — **ربحت في أوّل استعمال، وربحُها نفيٌ**

**قرار المالك 2026-08-04، بعد إصلاح `evalIn`:** *«شغّل كلَّ شيء وأبلغني بما احمرّ
— ولا تُصلح شيئاً»*، **وتوقّع موجةَ حمرة**. ⇒ ⛔ **فلم تقع، والسببُ أنفعُ من
الموجة: الإصلاح لم يبلغ قسم [٣] أصلاً** — `bench-options-page` **لا يستورد
`evalIn` المشتركة**، بل **يُعرّف نسختَه** منها.
⇒ ⭐ **ولو دُمج الإصلاحان كما عُرض أوّلاً لَظُنّ [٣] مُصلَحاً وهو على حاله** —
**وأخضرُه بعد الدمج كان سيُقرأ إصلاحاً**، وهو أخطر من بقائه أحمر.
⇒ **فالخطوةُ تُثبت نفسها حين تنفي توقّعَ من أمر بها، لا حين تؤكّده** — وهي أختُ
قرار 32: *القاعدة التي تُكتب بعد خطأ تُثبت نفسها حين تمنع الخطأ الثاني.*

### ⭐ وحصيلةُ إصلاح المُساعد: **رميةٌ واحدة في تسعةَ عشرَ رِكازاً — وهي تأكيدٌ لا خيبة**

**قرار المالك 2026-08-04، وصيغتُه تُكتب لأنها تُقرأ خطأً بالبناء:** من قرأ
«أُصلح مُساعدٌ في **253 نداءً**» ثمّ وجد **رميةً واحدة** ظنّ الإصلاح بلا طائل.
⇒ ⭐ **والصواب أنه أزال احتمالاً كان قائماً، وأنطق واحداً كان صامتاً.**
⇒ **وهو إثباتٌ بالعدد لحدٍّ كان مُعلَناً بالقول: «الابتلاعُ شرطٌ لازم لا كافٍ».**
**والرمية الوحيدة سُجّلت بندَها (#102)** — مسارٌ احتياطيّ `|| document.body`
يمكن أن يكون `null` هو الآخر.
⚠️ **ولا يُقرأ من صمت الباقي سلامة** (قرار المالك): **من بقي أخضر فإمّا لم يكن
مُضلَّلاً أو لم يبلغ حاله — والثاني يُسمّى ولا يُدّعى عنه سلامة.**

### ⭐ ما اشتراه `test-name-resolves` قبل أن يُحذف — **الحارسُ يزول والواقعةُ تبقى**

**حُذف 2026-08-04 بعد أن رُئي الأخُ الجديد (`no-undef`) أحمرَ على شاهده بنصّه**
(`options.js:722 ⇒ 'syncSpeedBadgeRow' is not defined`). ⛔ **وشاهدُه يُنقل قبل
الحذف لا بعده** (قرار المالك) — **فالثمنُ الذي دُفع لا يُعاد دفعُه:**

**ستّةُ عيوبٍ فيه يومَ مولده، كلُّها من المطابقة النصّية:**
1. **فحصُ كلّ ملفٍّ وحده** ⇒ **19 اسماً سليماً طُبع عطباً** — قِيس «أمعرَّفٌ في
   ملفّه؟» والمطلوب «أيُحلّ عند ندائه؟» (**قرار 81 داخل الحارس**).
2. **`strip` تبتلع أسطر القوالب** ⇒ انزاحت الأرقام والتصقت الرموز.
3. **ولا تعرف الحرفيّات النمطية** ⇒ `/(^|\.)youtube(-nocookie)?\.com$/` قُرئ
   **نداءً لـ`youtube`**، **ونمطٌ فيه علامةُ اقتباس فتح سلسلةً وهميّة فابتلع
   تعريفاتٍ بعده**.
4. **قائمةُ عوالمَ بلا بيئة** ⇒ **`chrome` مرّ في ملفّ عُقدة** — وهو #100 الثاني.
5. **نافذةُ 20 سطراً** لتصنيف مِجَسّات اللصق ⇒ **أخطأت في الاتّجاهين**.
6. **وتصنيفٌ بالنثر لا بالتشغيل** ⇒ `test-*.js` عُدَّت صفحةً وهي عُقدةٌ بالبناء.

⇒ ⭐ **ودرسُه الجامع، وهو ما يبقى: كنّا نُعيد بناء مُحلِّل نطاقاتٍ بمطابقاتٍ
نصّية، والمطابقةُ النصّية هي أصلُ عيوبه الستّة.** ⇒ **فقبل بناء أداة: أهذي مسألةٌ
عامّة لها حلٌّ مُجرَّب؟ الخصوصيّةُ نبنيها، والعموميّةُ تُستعار.**
⇒ **والمقيس الذي حسم: حرّاسُنا الثلاثة أخرجوا صفراً من صنفٍ أخرج فيه `no-undef`
ثلاثةَ عشرَ عطباً حيّاً في 0.8 ثانية.**

⛔ **ولم يُحذف `test-scope-reach`:** أُعيد عطبُه (`persistTiming` داخل دالّةٍ
أخرى) **فلم يُنتَج الشاهدُ أصلاً** — `node --check` صمت والحارسُ القديم بقي أخضر.
⇒ **فلا يُحذف ما لم يُرَ الأخُ أحمرَ على شاهده، ولو بدا مشمولاً.**
⛔ **و`test-vm-scope` يبقى** — مقيسٌ أن `no-undef` يُخرج عليه **صفراً**: `const`
داخل سكربت `vm` **دلالةُ تشغيل لا نطاقُ تحليل**.

### ⛔⭐ **قياسُ طرفٍ من علاقةٍ يصف نتيجةً ولا يُسمّي سبباً** (قرار المالك 2026-08-04)

**الواقعة:** `bench-host-volume` يقرأ `video.volume` **ولا يقرأ نموذج المضيف**
(`yt-player-volume`) — **فطبع «مُحيت»**. ⇒ **ولو قرأ الطرفين لقال «تباعدا»**،
⭐ **ولَفسَّر الشاذّة المسجَّلة قبل ستّة أيام بدل أن يتركها «غير مفسَّرة».**
**والمرشَّح الذي خرج بعد قراءة الطرفين:** المحوُ يقع **حين يتباعد العنصرُ عن
نموذج المضيف**، **وقيادةُ واجهته تُبقيهما متوافقين — والتوافقُ هو ما ينجو.**
**والمقيس:** بمحوّلنا `ع55/ن55` **متطابقان في كلّ عيّنة، ويصمدان إلى 8000ms وعبر
دورة كتمٍ من المضيف**. ⚠️ **وقيدُه: تشغيلةٌ واحدة ⇒ «مقيسٌ مرّةً ومتّسقٌ مع
الآليّة» لا «مُثبَت»** — **ولا يُكرَّر ما فعله «56.2%».**

⭐ **وأوضحُ شاهدٍ عليه جاء من المِجَسّ نفسِه:** سطرُه الأوّل قال «خطوة45» —
**وليست خطوتَنا**: كتب `0.5` مباشرةً فصار النموذجُ `0.95`. ⇒ **فأنتج المِجَسّ
التباعدَ ثمّ قاسه في السطر الذي بحثنا فيه عن الظاهرة نفسِها.**

### ⛔ **مقياسٌ للنوافذ عمي عن نافذة** — ثامنةُ اليوم (2026-08-04)

عُدَّت نوافذُ الشواهد بـ`sleep(\d+)` **ففاته جدولُ العيّنات**
`for (const at of [16,50,…,3000])` ⇒ **طبع `1200ms` لـ`bench-host-volume` وهو
يقيس إلى `3000`** — **وهو أهمُّ صفٍّ في الجدول، وأطولُ نافذةٍ فيه.**
⇒ ⭐ **أداةٌ بُنيت لقياس النوافذ عميت عن أطولها** — وأُمسك **قبل النشر** لأن
الرقم خالف خرْجاً مقروءاً بالعين.
✅ **والمقيس بعد التصحيح: عشرةُ شواهد تدّعي نجاةً أو ثباتاً، وستّةٌ نافذتُها دون
`3000ms`** — **وكلُّها مكتوبةٌ بيد، لا واحدةَ مشتقّةٌ من سلوك المضيف.**
⇒ **وصار لكلٍّ سطرُه في نصّه: «يقيس النجاة حتى Nms ولا شيء بعدها».**

### ⛔ وحدُّ مسحِ «المُتحفَّظ عليه ⇒ القاطع» — مُعلَنٌ بمُطلِقه ولا يُحسَّن

**المسحُ أخرج صفراً، والموضعان وُجدا بالقراءة** (`content.js:3298` ·
`CLAUDE.md:518`). ⇒ ⭐ **وأداةٌ تُخرج صفراً وقد فاتها ما وجدته العينُ تُوصف بما
فاتها لا بما أخرجت.** **والتحفّظُ لا يُكشف بكلماتٍ نعدّها نحن؛ المسحُ يبحث عن
صياغتنا لا عن المعنى.**
🔔 **ومُطلِقُه: ثالثُ موضعٍ يُكشف بالقراءة** ⇒ **حينها لا يصلح المسحُ النصّيّ
لهذا السؤال، فيُبنى غيرُه أو يُترك مُعلَناً.** ⛔ **ولا يُحسَّن قبل ذلك.**

### ⭐ **الزوالُ الذي لا يُفهم سببُه ليس إصلاحاً** (قرار المالك 2026-08-04)

**وهي أختُ «سلامةٌ لا نعرف سببها تسقط في أوّل تغيير».**
**الواقعة:** أُضيفت بيئةُ `serviceworker` فزال أحمرُ `importScripts` (14 ⇒ 13) —
**ولم يكن السببُ مفهوماً، فأُوقف كلُّ شيء ولم يُكوَّم.**
**وفرضيّةُ المالك — وهي أسوأُ الاحتمالات فقُيست أوّلاً:** «البيئةُ أسكتت الملفَّ
بدل أن تُعرّف اسماً — إسكاتٌ بثوب تهيئة».
⇒ **سقطت بالقياس:** أُدخل `vzNotARealName()` في `background.js` **فبُلِّغ**
(`35:1 error`). ⇒ **الملفُّ مفحوصٌ، والزوالُ سببُه آخر.**

⛔ ⭐ **والسببُ خطأٌ في قياسي أنا، من العائلة نفسِها ثالثةً في يومٍ واحد:**
فحصتُ `!!(g.serviceworker||{}).importScripts` **فقرأتُ `false`** — **وحزمة
`globals` تُخزّن `{ importScripts: false }` بمعنى «عالَمٌ للقراءة فقط»، لا
«غير موجود»**. ⇒ **قِستُ «أهي صادقةٌ في `if`؟» وأبلغتُ «أموجودةٌ؟»**
⇒ ⭐ **والفرقُ بين `false` و`undefined` هو الفرق كلُّه — وقد بُني حارسُ اليوم
على تمييز `undefined` من الرمية، ثمّ وقع كاتبُه فيه في سطرِ فحصٍ عابر.**
⇒ **فالتعليقُ في التهيئة صحيحٌ كما كُتب، والخطأُ كان في نفيي إيّاه** — **ولا
يُعاد تعليقٌ على ظنٍّ ثانٍ قبل أن يُعرف السبب** (قرار المالك: تعليقٌ ثانٍ على
ظنٍّ ثانٍ هو الخطأ نفسُه مرّتين).

### ⭐ **التوليدُ يمنع التباعد ولا يضمن الصواب** (قرار المالك 2026-08-04)

**الواقعة:** اشتُرط أن تُولَّد قائمةُ الأسماء المشترَكة **من `<script src>`** —
**فحُدّد المصدر ولم يُسأل: أيُنتج المجموعة الصحيحة؟** ⇒ **والتوليدُ من HTML
صادقٌ لدلالة المتصفّح (كلُّ سكربتٍ يرى إخوته) — فيجعل كلَّ اسمٍ في `options.js`
عالَماً في `storage.js`** ⇒ **ويُسكت الفحصَ عن اسمٍ حُذف وناداه أخوه، وهو #77
بعينه**، **بل ويُسكته عن `syncSpeedBadgeRow` نفسِه — شاهدِ #101**.
⇒ ⭐ **فالتوليدُ جوابٌ عن «أتتباعد القائمة؟» لا عن «أهي صحيحة؟»** — وهما سؤالان.

### ⛔⭐ **شرطُ قبولٍ للتبنّي كلِّه: علاجٌ يُسكت الحارسَ عن الصنف الذي بُني له أسوأ من غيابه**

**(قرار المالك 2026-08-04)** — **ولا يُقاس التبنّي بمكسبه بل بما لا يُفقده**:
**بلا رؤية #77 لا معنى للتبنّي أصلاً، و«13 × `chrome`» مكسبُه الثاني لا الأوّل.**
⇒ **ويُثبَت شاهدُ #101 (`syncSpeedBadgeRow`) بعد اختيار الطريق لا قبله** —
**فحذفُه اليوم لا يُحمّر شيئاً، وذاك بنفسه دليلٌ على أن الطريق الأوّل لا تصلح.**

⇒ ✅ **والمقيس الذي حسم الطريق: صفرُ نداءٍ صاعد وصفرُ مرجعٍ صاعد** —
`storage.js` و`settings-ui.js` **يُنادَيان ولا يُنادِيان**. ⇒ **فالطبقةُ
(مُقدِّمون · مستهلكون) وصفُ ما نفعله لا تضييقٌ لما يُتيحه المتصفّح.**
⭐ **وصياغةُ المالك أصحّ من صياغتي:** المسألةُ **«من يُقدّم لمن» لا «من يرى من»**.

### ⛔⭐ **أوّلُ أحمرَ لحارسٍ جديد يُقرأ سطراً سطراً قبل أن يُسكَت** (قرار المالك 2026-08-04)

**الواقعة (#101):** أوّلُ تشغيلٍ للحارس أعطى **19 اسماً**، **كلُّها سليمة** —
والعلّةُ فيه لا فيها. ⇒ **ولو أُضيفت استثناءاتٌ ليمرّ لَأخفت العيبَ بدل أن
تُصلحه**، **ولَبقي الحارسُ يمرّ على ما بُني ليمنعه.**
⇒ ⭐ **والاستثناءُ الذي يُضاف ليمرّ الحارسُ يقتله وهو حيّ.**
⚠️ **وهي أختُ «حارسٌ يُضعَّف ليمرّ عليه كودُنا حارسٌ ميّت» — لكن في لحظةٍ أخطر:
يومَ يُولد، حين لا أحد يعرف بعدُ ماذا يحرس**، فلا مرجعَ يُقارَن به ولا تاريخَ
يُنبّه. **وثمنُ القراءة يومَها دقائق، وثمنُ تركها عمرُ الحارس.**
✅ **والمقيس بعد القراءة: 19 ⇒ 2 ⇒ 0** — **وثلاثةُ عيوبٍ في الحارس نفسِه خرجت
منها**، أوّلُها **قرار 81 داخله**: قِيس «أمعرَّفٌ في ملفّه؟» والمطلوب «أيُحلّ عند
ندائه؟».

### ⚠️ وميكانيكٌ يُسجَّل ولا يُقرأ لوماً: **ما يُكتب لا يصل كلُّه**

**الواقعة (2026-08-04):** أُشير إلى أمرٍ بتسجيل تصنيف رمز الفشل **ولم يكن قد
وصل** — بُرومت قُطع. ⇒ **فالجواب «لم يُؤمر به» كان صادقاً، والامتناعُ عن افتراض
أمرٍ لم يُوجد هو الصواب.**
⇒ ⭐ **و«أمرتُ بكذا» ليس سنداً من المالك ولا من غيره — يُقاس بالسجلّ لا بالذاكرة.**
**وهي الثالثة من بابها في يومٍ واحد:** «قرار 85» (رقمٌ بلا مدخل) · «صفّ #81»
(نُفِّذ والسجلّ يقول مؤجَّل) · وهذي. ⇒ **فالذاكرةُ طرفٌ في الحوار لا مرجعٌ فيه.**

### ⭐ حجّةٌ صحيحة تُنتج نسبةً صحيحة **بالصدفة** (قرار المالك 2026-08-04)

**الواقعة:** عند نسبة أحمر `bench-91-title` و`94-preview` كانت الحجّةُ حاضرة
وصحيحة: **«إصلاحي يُحوّل ابتلاعاً إلى رمية، ولا يستطيع أن يمنع يوتيوب من بناء
صفحته»**. ⛔ **ولم يُعتدّ بها حتى قِيست** — عيّنةٌ ثانية على الكود نفسِه أعطت
`❌0` بعد `❌2` ⇒ **تباينُ مضيف** (شكل #97).
⇒ ⭐ **والقاعدة: الحجّةُ الصحيحة تُنتج النسبة الصحيحة بالصدفة، والقياسُ يُنتجها
بالضرورة** — **والفرقُ بينهما لا يظهر إلا حين تكون الحجّة خاطئة، وحينها يكون قد
فات.** ⇒ **فلا يُنسب أحمرُ بحجّةٍ مهما بدت قاطعة.**

### ⭐ وحصيلةُ الجرد: **ما يطمئن ليس النتيجةَ بل أنها قِيست**

**عقدُ الصوت (#60) وعقدُ الجودة (#19) والحاوية (#58 · #17 · #59) كانت محروسةً
كلَّها — وغيرَ مُشغَّلةٍ منذ أسابيع.** ⇒ **والجردُ لم يجد فيها عطباً، وليست تلك
حصيلتَه:** **حصيلتُه أن الفرق بين «محروس» و«مُشغَّل» كان مفتوحاً ولم نره.**
⇒ **ولو كان فيها عطبٌ لَنام كما نامت #103** — **والنتيجةُ الطيّبة هنا حظٌّ لا
دليلَ على سلامة النظام.**

### ⛔⭐ **امتناعٌ مُعلَنٌ لا يُلزم الكودَ الذي يليه** — شكلٌ سُمّي 2026-08-04 (#104)

**رِكازٌ يطبع «⇒ القياس غير صالح — لا يُبنى عليه بند» ثمّ يطبع في السطر التالي
حكماً مبنيّاً عليه** («❌ #91 قائم») — **فيتّهم بنداً مغلقاً مؤكَّداً من المالك.**
⇒ ⭐ **وهو أسوأ من الابتلاع: الابتلاعُ يصمت، وهذا يقول «لا تصدّقني» ثمّ يتّهم.**
⚠️ **وهو أخو #103 من بابٍ آخر:** هناك **حالةٌ تُسجَّل ولا مُنادي**
(`pendingRevertSeq` معلَّقٌ بلا `flushPendingRevert`)، وهنا **امتناعٌ يُعلَن ولا
مُلزَم**. ⇒ **وكلاهما «قِيل ولم يُنفَّذ»** — والقولُ وحده لا يُغيّر مساراً.

### ⛔⭐ جردُ الشواهد 2026-08-04 — **والعدّاد الذي بنيتُه لأكشف به كان أعمى بالشكل نفسه**

**37 رِكازاً · 28 منها شاهدُ قبولٍ لبندٍ مغلق · و26 خارج البوّابة يومَها.**
⛔ ~~**«11 شاهداً أحمرَ نائماً»**~~ — **رقمٌ مسحوب (قرار 21)، وسببُ سحبه هو الدرس:**
**رمزُ `❌` يحمل أربعة معانٍ، وعدّادي جمعها في رقمٍ واحد:**

| `❌` يعني | مثالُه المقيس |
|---|---|
| **المنتَجُ سقط** | `bench-s69-guards` — **واحدةٌ فقط، وهي #103، وأُصلحت** |
| **امتناعُ الرِكاز** («لا يُبنى عليّ») | `86-fs` · `94-preview` · `s6-click` (تويتش) · `kick-slider` |
| **حقيقةٌ مقيسة عن المضيف** | `yt-mute-step` («سلوك مضيف») · `twitch-slider` · `yt-adapter` |
| **نثرٌ يذكر الرمز** | `repro-59-body`: «اقرأ `KNOWN-DEFECTS` قبل تفسير أي ❌» |

⇒ ⭐ **عدّادٌ يعدّ ما يستطيع لا ما نحتاج — وهو ما نطارده في الحرّاس، واقعاً في
أداة الجرد التي بُنيت لتكشفه.** ⇒ **ولا يُقرأ رقمٌ منه إلا بعد قراءة نصّ كل خرج.**

**⇒ والجواب بعد القراءة: عطبٌ حيٌّ واحد كان نائماً (#103)، وأُصلح. ولا ثانيةَ.**

⭐ **وما طمأن ليس النتيجة بل أنها قِيست** — **وعُرفت لأن المالك أمر بالجرد، لا لأن
شيئاً كان يُشغّلها**. وهو ما يُصلحه شرطُ رفع النسخة أعلاه.

✅ **وعقودُنا الكبرى قِيست في الجرد نفسِه ووُجدت قائمة:** #60 (أربعةُ رِكازات:
المستوى ينجو من دورة الكتم · وتويتش يفكّ الكتم بمنزلقه `0%⇒80%`) · #19 (صفرُ
ضبطٍ على الإعلان و2 على المحتوى) · #58 و#17 (العشرُ بنيات · «وُسِمت 3 · رُفضت 7»
· صفرُ دخولٍ احتياطيّ) · #59 (`ما تُرجعه الإضافة: VIDEO`).

⚠️ **وثلاثُ نسبٍ خرجت بالقياس لا بالحجّة:** `86-fs` **سابقٌ لنا** (أحمرُ قبل
الإصلاح وبعده) · و`91-title` و`94-preview` **تباينُ مضيف** — **كودٌ واحد أعطى
`❌2` و`❌0`، و`❌1` و`❌0`** (شكل #97). ⭐ **وكانت لي حجّةٌ صحيحة لم أعتدّ بها
حتى قِيست:** «إصلاحي لا يستطيع أن يمنع يوتيوب من بناء صفحته» — **و«لا يستطيع»
استنتاجٌ لا قياس.**

### ⛔⭐ **شاهدٌ لا يُشغَّل شاهدٌ لا وجود له** (قرار المالك 2026-08-04، من #103)

**وهي تنقض ظنّاً عملنا به طويلاً: أن كتابة الشاهد تكفي.**
**الواقعة بأرقامها:** قسم (٣) في `tools/bench-s69-guards.mjs` **كُتب في كومِت
#69 نفسِه (`d073eae`) شاهداً على الإصلاح** — **فاحمرّ بـ#78 (`8b8be41`) وبقي
أحمرَ أسابيع ولم يره أحد**، لأن رِكازه **ليس من الأربعة التي تُشغَّل قبل كل كومِت**.
⇒ ⭐ **والشاهدُ كان قائماً يقول، والعطبُ عند المستخدم، ولا سامع.**
**وهذي الجملةُ هي القاعدة لا شرحُها** — بها يُعرف لماذا «شاهدٌ لا يُشغَّل شاهدٌ
لا وجود له» **ليست مبالغة**: الحارسُ لم يسكت ولم يخطئ، **وأخضرُنا لم يكن يشمله**.
⇒ ⭐ **فالكتابةُ نصفُ الحارس، والتشغيلُ نصفُه الآخر** — **وحارسٌ يُشغَّل عند
الطلب هو حارسٌ يُشغَّل حين نظنّه لازماً**، وهو عينُ ما أسقطه قرار 32 في نصّه
الثالث. ⇒ **ولكلّ شاهدِ قبولٍ لبندٍ مغلق أحدُ ثلاثة، ولا رابعَ:**
**(١) يُنقل إلى `run-tests`** إن كان رخيصاً حتميّاً ·
**(٢) أو يُضاف إلى الأربعة** إن لزمه متصفّح ·
**(٣) أو يُعلَن «يُشغَّل عند الطلب» بسببٍ مكتوب** — ⛔ **والثالث مقبولٌ إن كُتب
سببُه، مرفوضٌ إن كان سكوتاً.**

### ⭐ وثلاثةٌ من علاج #103 — **شكلُ التوحيد لا تفصيلُه**

1. ⭐ **«أوسع أم أضيق» يُقاس لا يُقدَّر** (قرار المالك 2026-08-04، وقد أمر
   بالتوحيد **وهو لا يعرف أيّهما**): قِيس النطاق قبل القرار — **موضعان يرفعان
   الحارس وواحدٌ يُفرّغ الإرجاع** ⇒ **التوحيد أضيق من العلاج**، فنُفِّذ.
   ⛔ **ولو خرج أوسعَ لَقيل ولم يُنفَّذ** (قرار 16) — **والشرطُ يُقاس لا يُطاع.**
2. ⭐ **وكلُّ فرقٍ بين النظيرين يُسمّى قبل التوحيد:** `persistCleanPlayer` كانت
   **تُبلّغ التبويبات حتى عند الفشل** والمُوحَّد لا يُبلّغ — **وأثرُه صفرٌ
   بالبناء** (الرسالة تُعيد قراءة تخزينٍ لم يتغيّر). ⇒ **وهذا ما يُميّز توحيداً
   مقيساً من توحيدٍ يُسقط سلوكاً بلا أن ينتبه أحد.**
3. ⭐ **والحارس يعدُّ ولا ينادي:** «موضعٌ واحد يرفع، وواحدٌ يُسقط، والتفريغ في
   `finally` نفسِه» — **لا «النداء موجودٌ في الاثنين»**، فتلك تمرّ على **ثالثةٍ
   تُكتب غداً**. ⇒ **وهو الفرق بين حارسٍ يمنع عودةَ السطر وحارسٍ يمنع عودةَ
   العطب.**

### ⭐ والنسبةُ نفسُها قياسٌ يخضع لقواعد القياس (قرار المالك 2026-08-04)

**الواقعة:** نُسب أحمرُ `bench-zero-change` بإعادة تشغيله على المُساعد السابق،
**فأعطى `❌ = 0`** — **وكان سيُقرأ «كان أخضر فاحمرّ بإصلاحك»**.
⛔ **والنصُّ يقول غيره: «⚠️ لم يُقس أي موقع»** — المواقع الأربعة كلُّها «لا فيديو».
⇒ **فصفرُه لأنه لم يُقارن شيئاً، لا لأنه سلِم** — **الشاهد الأوّل في قرار 26
(صفرٌ لا يُفصل مصدره) واقعاً في النسبة لا في الرِكاز**.
⇒ ⭐ **ونحن نُطبّق قرار 26 على ما نقيسه، وهذي أوّل مرّةٍ نُطبّقه على ما نقوله عن
قياسنا.** **والفرقُ بين «كان أخضر فاحمرّ بك» و«لم يقس شيئاً» هو الفرق بين اتّهامٍ
وبراءة** — **ولم يمنعه إلا أن النصّ قُرئ بعد العدد.**

### ⭐ ورقمُ قرارٍ بلا مدخلٍ يحمله — **بابٌ رابع في «النجاح الكاذب»، وقع في سجلّنا**

**الواقعة (2026-08-04):** استُشهد بـ«قرار 85»، **فبحثتُ عن `^85.` فلم أجده
فأبلغتُ «لا وجود له»** — **والنصُّ كان حاضراً**، مكتوباً في `24df2ea` بتاريخه
ونسبته **داخل متن قرار 82**، وأُعطي الرقم بلا أن يُنشأ له مدخل.
⇒ ⭐ **رقمٌ يُعطى بلا مدخلٍ يحمله يصير حجّةً لا تُوجد** — وهو قرار 34 في الأرقام
مطبَّقاً على القرارات نفسها.
⛔ **وخطئي فيها مُسجَّل: قِستُ «أثمّة مدخلٌ مرقَّم 85؟» وأبلغتُ «أثمّة قرار 85؟»**
— **وهما ليسا شيئاً واحداً**. ⇒ **«المقيسُ جارُ المطلوب» (قرار 81) واقعاً في
قراءة سجلّنا لا في رِكاز**، **وأوّلُ وقوعٍ له خارج الأدوات.**
⭐ **وما أنقذ الموقف امتناعٌ لا قياس:** رُفض أن يُنسَب الحدُّ إلى رقمٍ لا سند له،
**فكُتب بنصّه حتى يُحسم** — **وهو قرار 16 مطبَّقاً على المالك كما على غيره**،
وبه انكشفت الملابسة بدل أن تُدفن. **والمدخلُ أُنشئ ونُقل إليه النصّ (لا نُسخ).**

⛔ ⭐ **وأخطرُ ما فيها لم يقع، وكاد: إقرارٌ بذنبٍ لم يُرتكب.** كتب المالك على نفسه
«رقمٌ اخترعتُه» **لأن التقرير بدا قاطعاً** — **والنصُّ كان مكتوباً بتاريخه ونسبته**.
⇒ **ولم يمنعه إلا أن البحث وقع في النصّ لا في الفهرس.**
⇒ ⭐ **فالاعترافُ السريع ليس دقّةً — وهو مقلوبُ الدفاع السريع، وكلاهما يُغني عن
القياس** (قرار المالك 2026-08-04). ✅ **والإقرارُ لم يبلغ السجلَّ فبقي في
المحادثة؛ وما بلغه هو الدعوى الخاطئة في صفّ #101، فشُطبت بسببها (قرار 21)** —
**وإقرارٌ كاذبٌ في السجلّ أسوأ من نسيان، لأنه يُصدَّق ولا يُراجَع.**

⇒ ⭐ **وتُكتب صريحةً لأنها كانت مضمَرة: قرار 81 ليس عن الرِكازات — هو عن كلّ
إخبارٍ عن قياس**، ومنه إخبارُنا عن سجلّنا. **وهذي أوّل مرّةٍ يقع خارج الأدوات.**

🔔 **ومرشَّحُ حارسٍ رُدَّ عمداً لا سهواً — يُكتب كي لا يُقرأ نسياناً بعد سنة:**
«كلُّ `قرار N` يُستشهد به في أي ملفّ يجب أن يحمله مدخلٌ `N.`» — **عُرض، ومال
إليه المالك، ثمّ ردَّه بقاعدته هو**: **الواقعة واحدة، وقرار 85 يشترط تكراراً
مرصوداً.** ⇒ ⭐ **وأوّلُ اختبارٍ لقاعدةٍ أن تُطبَّق حين تُغري بالمخالفة.**
**ومُطلِقُه: ثاني استشهادٍ برقمٍ بلا مدخل.**

### ⭐ ونسخةٌ محلّية من مُساعدٍ مشترك — **داؤنا في المنتج، وأوّلُ مرّةٍ في الرِكاز**

**لاحقناه في المنتج مراراً** — عدّادُ التأكيدات في أربعة مواضع (قرار 34) ·
المُولِّدان · الفروع الثمانية — **وهذي أوّل مرّةٍ يُمسك في أدواتنا**:
`bench-options-page.mjs` يُعرّف `evalIn` و`evalIn0` محلّياً **وسبعةَ عشرَ ملفّاً
غيرُه يستوردان المشتركة**. ⇒ ⭐ **والأدواتُ لا تُعفى من قواعد المنتج.**
⇒ **والعلاجُ حذفُ النسخة لا إصلاحُها** (قرار المالك): تستورد المشتركة كما يفعل
غيرُها — **وإن كان فيها فرقٌ مقصود فيُسمّى ويُقال لماذا، ولا تبقى نسخةٌ بلا سبب**.

### ⭐ والصمت ليس نجاحاً — شاهدان من هذه الجلسة

- **`node --check` تمرّ على صفحةٍ ميتة**: تفحص النحو لا المراجع وقت التشغيل.
  **1808 تأكيداً و43 فحصاً خضراء وصفحة الإعدادات لا تُفتح** (#77).
- **و`String.replace` لا تُخبر حين لا تُطابق**: حُذفت دالّة مع كتلةٍ أوسع، ثمّ
  مرّ الاستبدال اللاحق **صامتاً**. ⇒ **كل استبدالٍ آليّ يُتحقَّق من وقوعه.**
  > ⚠️ **ولا يُظنّ أن هذه القاعدة تكفي (#82، 2026-08-03):** العَرَض نفسه
  > (`ReferenceError` في `options.js`) خرج له **سببان مختلفان** — في #77 **دالّةٌ
  > حُذفت**، وفي #82 **دالّةٌ موجودةٌ في نطاقٍ لا يراه المُنادي**. **ولا شيء
  > استُبدل في الثانية ولا حُذف؛ الكودُ كلُّه حاضر والنطاق هو الخطأ.**
  > ⇒ **فالقاعدة تُمسك الأولى ولا تُمسك الثانية، ولم يُمسكها إلا حارس**
  > (`tools/test-scope-reach.js`). **وعَرَضٌ واحد لا يدلّ على سببٍ واحد** (قرار 42).

- **وحارسٌ يفحص الدالّة ولا يفحص استعمالها يحرس ما لا يُستعمل** (2026-08-04،
  من #94). حارس البوّابة كان يمرّ كاملاً على منتجٍ **حُذفت منه البوّابة**، لأنه
  ينادي الدالّة بنفسه. ⇒ **فأُضيف قسمٌ يحرس موضع النداء**، و**حذفُ البوّابة
  يُحمّره**. ⭐ **وهي أختُ الأولى بنصّها**: `node --check` تفحص **النحو** لا
  المراجع، وحارس الدالّة يفحص **الدالّة** لا استعمالها — **وكلاهما يفحص ما يستطيع
  لا ما نحتاج**.
- ⭐ **وما يجعل الحارس مُثبَتاً لا مُدّعىً: شاهدُه على العطب نفسه.** حارسا #94
  و#96 يُشغَّلان على **مصدرٍ مُفتعَل بالكود السابق** فيريان الشريط الأجنبيّ
  **يُصاب وهو مرئيّ 304×40** — **فالعطب كان حيّاً لا نظرياً**، والخضرة بعده خبرٌ
  لا دعوى (قرار 47).

- ⭐ **و«غيابُ الخبر ليس خبراً بالصفر» في موضعٍ جديد (2026-08-04، #86):** حدثٌ
  **بلا `buttons`** لا يُحكم به في التوفيق — **فلا يُقرأ غيابُ الحقل «لا أزرار
  مضغوطة»**. وهو الشكل نفسه في `S7` و«لم أقس ≠ قِستُ فوجدت صفراً».
- ⭐ **وبابٌ خامس لعائلة الأصفار: «لم يقع» و«وقع مرّتين فعاد» يطبعان `null`
  نفسَه** (2026-08-04، من #86). قرأتُ `document.fullscreenElement === null` بعد
  نداءٍ ثانٍ **فكتبتُ «الإيماءة لا تُحتسب من `Input`»** — **وقد وقع الدخول فعلاً
  ثمّ خرج**، لأن الأمر **قلبٌ لا دخول**. ⇒ **فالمقلوب يُقاس بالحالة قبلَه لا
  بالحالة بعده** — والحدّ الباطل سُحب بقرار 21 حيث كُتب.
- ⭐ **والإثباتُ الكاذب أخطر من الصفر الكاذب** (2026-08-04، من #92): مطابقةٌ
  أوسع من سؤالها طبعت «ثمّة عنصر» عن **عنوان فيديو فيه «Rescued»**. ⇒ **الصفرُ
  الكاذب يُغلق بحثاً، والإثباتُ الكاذب يفتح بناءً** — **وكان سيُنتج مفتاحاً يَعِد
  بما لا يفعل**، وهو ما صُرف #66 و#67 في إزالته.

⇒ **حارسٌ يفحص ما يستطيع لا ما نحتاج، وسكوتُه يُقرأ سلامة.**

---

## 4. قرارات المالك — **الأحدَ عشرَ التي تُستعمل في كل جلسة**

> **والمعيار: لو غاب القرار عن هذا الملفّ، أيقع الخطأ في جلسةٍ جديدة؟**
> **نعم في الأحدَ عشرَ كلِّها، وقد وقع فعلاً في كلٍّ منها.**
> **وبقيّتها (67 قراراً) في [`docs/DECISIONS.md`](docs/DECISIONS.md)** —
> **تُقصد عند بندها ولا تُقرأ في كل جلسة.**

⭐ **ولكلٍّ واقعتُه — وهي ما يمنع أن يُخرجها أحدٌ بعد سنة اختصاراً** (قرار المالك):

| القرار | **الخطأ الذي وقع بغيابه** |
|---|---|
| **16** | طُلب نقلُ خطواتٍ «إن لم تكن نُفِّذت» — **وكانت نُفِّذت**، فكاد يُنشأ معلَّقٌ وهميّ |
| **19** | عطبُ #58 رُئي على أكثر من موقع **ولم تُسجَّل أسماؤها فضاع نصف الدليل** |
| **20** | تثبيتُ عطبٍ يترك المجموعة حمراء — **ومجموعةٌ حمراء دائماً مجموعةٌ ميتة** |
| **21** | انقلابُ الحالة أ عاش في ستّة ملفّات بعد سقوطه · **وتكرّر مرّتين في جلسةٍ واحدة** (حدٌّ باطل · «زالت كلُّها») |
| **22** | `450ms ⇒ 1100ms` — **أثرُ توقيت ارتقى إلى «حقيقة منشورة»** |
| **26** | عيبا رِكاز في يومٍ واحد، **وكلاهما كان سيصير حقيقةً منشورة** |
| **32** | كومِتُ تسجيلٍ وقع **والمجموعة خضراء و`audit-status` حمراء** |
| **34** | عددُ التأكيدات عاش في **أربعة مواضع، وأربعتُها خطأ** |
| **47** | `test-scope-reach.js` **طبع أخضر على عطبٍ مقيسٍ بين اليدين** |
| **48** | قِيس «اختفى الشريط» فحُسب إخفاءَنا **وهو إخفاء يوتيوب** (#86) · وتكرّر في #91 |
| **58** | «لم أمسّ `options` فالفحص غير لازم» — **والصفحة كانت تحمل `ReferenceError`** |

16. **كل شرط بصيغة «إن كان كذا» يُقاس على الشجرة والسجل قبل تنفيذه.** إن خالف
    الواقعَ فأبلِغ المالك **ولا تنفّذه**. مصدر القاعدة واقعة حقيقية: طُلب نقل
    خطوات اختبار #56 و#24 إلى قسم «مُعلَّق» **إن لم تكن نُفِّذت** — وكانت قد
    نُفِّذت وأُكِّدت، فنقلها كان سيُنشئ **معلَّقاً وهمياً**. التنفيذ الأعمى لشرط
    معلَّق على واقع هو خطأ، لا طاعة.

19. **كل بند يُسجَّل في `AUDIT.md` من الآن يحمل الرابط الذي رُئي عليه. بند بلا
    موقع مُسجَّل لا يُقبل.** سبب القرار أن المالك رأى عطب #58 على أكثر من موقع
    ولم يسجّل أسماءها، فضاع نصف الدليل. **وقبل الإبلاغ عن أي عطب ملء شاشة يُلصق
    `tools/report-fullscreen-bug.js` في كونسول الصفحة** — يطبع سطراً واحداً
    قابلاً للنسخ فيه الرابط والحاوية المختارة ومستطيلها ومستطيل الفيديو والنسبة
    وحكم البوابة. وهو تشخيصيّ **لا يُشحن**، ونسخته من منطق `pickFullscreenContainer`
    محروسة بـ `tools/test-fs-report-sync.js`.

20. **العطب المعروف يُثبَّت في مجموعة الاختبارات ويُبرهَن في منصّة القياس،
    ومجموعة الاختبارات تبقى خضراء دائماً.** قيمة المجموعة كلها في أن الأحمر
    يعني «كسرتَ شيئاً **الآن**» لا «شيء مكسور منذ أسبوع» — **مجموعة حمراء دائماً
    مجموعة ميتة**. وكل تثبيت عطب يحمل فوقه نصّاً صريحاً: «فشل هذا التثبيت يعني أن
    `<البند>` أُصلح، فحدّثه ولا تُصلح الاختبار». والعطب المعروفة كلها في موضع
    **واحد** يُقرأ قبل تشغيل المنصّة: `tools/KNOWN-DEFECTS.md`.

21. **الرقم المسحوب يُسحب من كل موضع ذُكر فيه، في الكومِت الذي يسحبه نفسه.**
    **رقم ميّت في ملف ثانٍ أخطر من رقم خاطئ في ملف واحد** — لأن الأول يُقرأ حجّةً
    وقد سقط سنده. ويُترك **مشطوباً بسببه** لا محذوفاً صامتاً، كي يعرف من يقرأ
    لاحقاً أنه **سُحب** لا أنه لم يُكتب. أول تطبيق: انقلاب الحالة أ (2026-07-30) —
    سُحب من `AUDIT.md` و`HANDOFF.md` و`KNOWN-DEFECTS.md` و`content.js` و
    `test-container-choice.js` و`repro-58-fullscreen.mjs` في كومِت واحد.
    **ولا تُبقَ في السجل حجّة لا تسندها قياسة:** قرار #58 كان على شاهدين فسقط
    أحدهما، فصُحّح تبريره إلى «هشاشة سكور ناعم مقيسة» بدل «انقلاب مرصود».

22. **كل قياس في أي منصّة يشترط استقرار التخطيط ومستطيلات غير صفرية قبل أن
    يُقرأ.** درس `450ms ⇒ 1100ms`: **أثر توقيت ارتقى إلى «حقيقة منشورة» لأن
    القياس قرأ قبل أن يستقرّ الشيء المقيس** — `body` بعد `textContent=""` يبقى
    بارتفاع 0 لحظةً، فيُرفض بحارس المستطيل الصفري ويفوز مرشّح آخر. من يكتب منصّة
    قياس ينتظر التخطيط **ويتحقّق أن المستطيل غير صفري** قبل أن يبني على الرقم.

26. **كل رِكاز قياس يُثبت أنه يرى قبل أن يُصدَّق: شاهد موجب معلوم النتيجة وشاهد
    سالب معلوم النتيجة، قبل أي رقم يخرج منه. ولا يُنشر رقم من رِكاز بلا شاهدين.**
    **مصدر القاعدة عيبا رِكاز حقيقيان في يوم واحد (2026-07-30)، وكلاهما كان
    سيصير حقيقة منشورة:**
    - **رِكاز المعزّز:** مذبذب يُبَث كـ`MediaStream` قرأ **صفراً تامّاً**، فكاد
      يُنشر أن «الكتم يُسكت المعزَّز» — والسبب أن كروم لا يوصّل
      `createMediaElementSource` لعنصر مصدره MediaStream. **الشاهد الموجب الذي
      كشفه:** مِجَسّ على المذبذب نفسه قرأ **0.353** بينما الرسم يقرأ صفراً.
    - **رِكاز محوّل يوتيوب:** «هل تفسد ضغطتنا نصّ المستخدم؟» اختُبر على حقل
      **فارغ** — لأن `Input.dispatchKeyEvent` لا يكتب حرفاً — فكان التقرير سيقول
      **«آمن ✅»** والسؤال **لم يُختبَر أصلاً**. **حقل فارغ لا يُثبت سلامة نصّ.**
      وبعد التصحيح بـ`Input.insertText` ظهر الخطر فوراً.

    **الشاهدان عملياً:** رقم **يجب أن يتغيّر** ورقم **يجب ألا يتغيّر**، كلاهما
    معلوم النتيجة سلفاً. **الصفر خاصةً لا يُصدَّق حتى يُفصل مصدره:** «لا يوجد»
    و«لا أرى» يطبعان الرقم نفسه.
    ⭐ **وفهرسُ العمى الخمسة — الشواهد مُلخَّصةً، وبها يبقى المبدأ مسنوداً:**
    1. **صفرٌ لا يُفصل مصدره** — «لا يوجد» و«لا أرى» يطبعان الرقم نفسه.
    2. **حالٌ لم تُنتَج** — حقلٌ فارغ لا يُثبت سلامة نصّ، و«لم أقس» ليست «قِستُ فوجدت صفراً».
    3. **مدىً لا يتّسع للحركة** — «لا مكان للصعود» و«لا يصعد» يطبعان الرقم نفسه.
    4. **نفيٌ ببصرٍ غير مُثبَت** — النفي يشترط أن تكون الآلةُ قادرةً على الرؤية أصلاً.
    5. **مقلوبٌ يُقرأ بالحالة بعده** — «لم يقع» و«وقع مرّتين فعاد» يطبعان `null` نفسه.
    ⇒ **والشواهد العشرون في [`docs/DECISIONS.md`](docs/DECISIONS.md) — تُقرأ حين
    يُكتب رِكاز، لا في كل جلسة.**

32. **المجموعة كاملةً تُشغَّل قبل الكومِت لا بعده** (قرار المالك 2026-08-01).
    ✅ **وأثبت نفسه في أوّل شهر (2026-08-02، #66):** أُعيد `content.js` بـ
    `git checkout` بعد افتعال تفرّق للتحقّق من الحارس، **فسقط معه حذف #66 نفسه** —
    والشجرة بدت سليمة والحارس أخضر (لأن السجلّين عادا متطابقَين على **الحالة
    القديمة**). **وما كشفه هو تشغيل المجموعة قبل الكومِت** ومعه `git status`:
    ملفٌ مُتوقَّع تغيّره لم يتغيّر. **ولو كُوِّم قبل التحقّق لكان الكومِت يحمل
    عنوان «حذف مفتاح multicam» بلا حذف** — نقطة إرجاع كاذبة بنصّها.
    ⇒ **والقاعدة التي تُكتب بعد خطأ تُثبت نفسها حين تمنع الخطأ الثاني لا حين
    تُكتب** (قرار المالك 2026-08-02).
    **ليست أدباً بل صحّة تاريخ:** كومِت يسبق التحقّق **يجعل نقطة الإرجاع كاذبة** —
    يبدو سليماً وهو ليس كذلك، فمن يرجع إليه يرجع إلى كسر. **وإصلاحٌ يلحق في كومِت
    بندٍ آخر يخلط بندين في تاريخ واحد**، فيصير `git revert` لأحدهما مستحيلاً بلا
    جرّ الآخر. **مصدرها واقعة:** كُوّم `a413277` (#38أ+ب) قبل تشغيل المجموعة،
    فظهر كسر `test-middle-click` بعده ولحق إصلاحه في كومِت #30.

    ⚠️ **النصّ الثالث — وُسِّع 2026-08-03 بقرار المالك، بعد سلبةٍ ثانية:**
    **التحقّق لا يُستثنى منه شيء بحكمٍ على ما لُمس.** عند كومِت §17 كُتب:
    «`bench-options-page` غير لازم — لم أمسّ `options.*` ولا `settings-ui.js`»،
    **وكان الحكم خاطئاً**: الصفحة كانت تحمل `ReferenceError: persistTiming is
    not defined` **وقتها**، ولم يكشفه شيء عندنا — كشفه **زرّ «أخطاء» في
    `chrome://extensions` عند المالك**.
    ⇒ **وهو عين شكل قرار 32 قبل توسيعه الأوّل:** *حارسٌ يُشغَّل حين نظنّه لازماً
    هو حارسٌ يُشغَّل حين لا نحتاجه ويُترك حين نحتاجه.*
    ⇒ **فالتحقّق من الآن أربعة، بلا استثناءٍ ولا اجتهاد:**
    `run-tests` · `audit-status` · **`bench-options-page`** ·
    **`bench-overlay-layer`** — **والأربعة تُشغَّل، ويُقارَن أحمرُ الفحصين
    بـ`tools/KNOWN-DEFECTS.md` (قرار 20): ما زاد عليه يمنع الكومِت.**
    **والقاعدة الجامعة: شرطُ التشغيل هو الكومِت نفسه لا تقديرُ من يكوّم.**
    ⛔ ~~**والفحصان أحمران اليوم بعطبٍ مقيسٍ مسجَّل**~~ — **مسحوبٌ 2026-08-04
    (قرار 21): أُغلق العطب بـ#72 و#82، والفحصان أخضران بالقياس.** **والقاعدة
    تبقى بنصّها:** من قرأ أحمرَهما إذناً بالتخطّي عاد إلى العلّة، ومن قرأه
    انحداراً أوقف عملاً سليماً. **يُقارَن بـ`tools/KNOWN-DEFECTS.md` دائماً.**

    ⭐ **وعلّةُ التوسيع صُحّحت بالقياس، والتصحيح أثمن من القرار:** ظُنّ أن الحارس
    **أُهمل**، فتبيّن أنه **ناقص** — مرّ **أخضر 6/6** على البناء الذي تحمل صفحتُه
    `ReferenceError`. **بنصّ المالك: «ظننتُ العلّة في ما لا نراه وهي في ما لا
    نفعله».** ⇒ **حارسٌ يقيس التحميل ولا يقيس التفاعل يقيس نصف المنتَج**،
    و**عيّنةٌ من ضابطٍ واحد تُثبت مسارَها لا الصفحة**.

34. **كل رقم في التوثيق يُشتقّ من مصدره أو يُحذف** (قرار المالك 2026-08-01).
    **رقمٌ يُكتب بيد يتباعد عن الحقيقة بالبناء لا بالسهو** — وهذا **درس البوّابات
    الإحدى عشرة نفسه مطبَّقاً على السجلّ بدل الكود** (قرار 16ج): حيث أمكن، اجعل
    الخطأ مستحيلاً بموضع واحد بدل أن تُحصيه في قائمة.

    **والواقعة بأرقامها:** عدد التأكيدات كان في **أربعة مواضع وكلّها كاذبة** —
    §7 قال **1169 في 37**، و§0 قال **1325 في 44**، و§1 قال **992 في 33**، ورقم
    بلاغيّ رابع في قرار 20 قال **949** — **والمقيس على الكومِت نفسه `1301` في 44**.
    ⇒ **`HANDOFF.md` الذي يُقرأ قبل كل جلسة كان يكذب في رقمه المركزيّ.**
    **ورقم متضارب أخطر من رقم غائب:** الغائب يُسأل عنه، والمتضارب يُبنى عليه —
    وأربعةٌ متضاربة تعني أن كل قارئ يبني على رقم مختلف.

    **⇒ المطبَّق:** `tools/run-tests.js` هو الموضع الواحد للعدد، و`--list`
    يشتقّ وصف كل ملف من سطره الأول، **ولا قائمة تُحدَّث بيد بقيت في §7**.
    والمشغّل **يفشل على ما لا يفهم خرْجه** لا يتخطّاه: مشغّل يعدّ ما يفهمه ويسكت
    عمّا لا يفهمه يطبع مجموعاً أصغر من الحقيقة **ويبدو أخضر**.
    **وكل رقم جديد يُكتب في التوثيق يُسأل عنه: من أين يُشتقّ؟ فإن لم يكن له مصدر
    يُطبع منه فلا يُكتب.**

47. **الحارس يحتاج شاهدَي قرار 26 كما يحتاجهما الرِكاز** (قرار المالك 2026-08-03).
    **الواقعة:** `tools/test-scope-reach.js` **طبع أخضر على عطبٍ مقيسٍ بين يديّ** —
    بحث عن حاضنةٍ **دالّةٍ مسمّاة**، والحاضنة **سهمية**.
    ⇒ ⭐ **وهذا أخطر ما يفعله حارس: يُجيز ما بُني ليمنعه، ويُسكت السؤال معه.**
    **فلا تُصدَّق خضرةُ حارسٍ حتى يُثبَت أنه يُحمّر على العطب المعلوم** — وشرطُ
    القبول في `#82`: **يُشغَّل على العطب قبل الإصلاح ويُرى أحمرَ**، لا بعده.

    > ⭐ **وسجّله المالك على نفسه 2026-08-04 (#93):** أقرّ حدَّ
    > `VZ_PLAYER_SCOPE_MAX_AREA = 1.2` بحجّة **«حارسٌ لا مُميِّز — لم يقلب حكم
    > أيٍّ من الخمسة»**، **وقبِل «لم يقلب» جواباً ولم يسأل «أبلغَته بنيةٌ أصلاً؟»**
    > — **وهو عينُ ما ظلّ يطلبه من غيره: السكونُ يحتاج إثباتاً أن ثمّة مدىً
    > يُتحرَّك فيه** (الشاهد الثالث في قرار 26).
    > ⇒ **فالقاعدة على حدٍّ أقرَّه المالك كما هي على حارسٍ كتبناه** — والحدُّ
    > صار مُجرَّباً ببنيةٍ تتجاوزه في `tools/test-numeric-bounds.js`.

48. **حين يفعل المضيفُ ما نفعله، لا يُقاس الأثر بل يُقاس سببُنا نحن**
    (قرار المالك 2026-08-03). **الشريط مخفيّ — أبِنا أم بيوتيوب؟ الأثر لا يُجيب،
    وصنفُنا يُجيب.** فمِجَسّ #70 قاس `opacity === 0` **فطبع فشلاً لا وجود له**:
    يوتيوب يُخفي شريطه بنفسه بعد ~3 ثوانٍ، والقياس ينتظر 4.5.
    ⇒ **صار يقيس `vz-idle-hide-progress`**، والشفافية خبرٌ تابع.
    ⭐ **والقاعدة اكتُشفت مرّتين قبل أن تُسمَّى:** حين حذّر المالك أن **ABR يغيّر
    الجودة من تلقائه** بُني الشاهد على **ردّ المستمع لا على القيمة** — وهي هذه
    بعينها. ⇒ **وتسري على كل ميزةٍ تُشارك المضيفَ فعلاً: الإخفاء والجودة والصوت.**
    **فقبل أي مِجَسّ: هل يستطيع المضيف أن يُنتج هذا الأثر وحده؟ فإن استطاع،
    فالمِجَسّ على سببنا لا على أثره.**

    > ⭐ **وأخصُّ أبوابها وقع 2026-08-04 (#86)، وحسمه المالك بنفسه بلا أن يُطلَب:**
    > الفرعان اللذان صيغا للمالك (**«حركةٌ بلا ضغط: اختفى ⇒ مؤشّرٌ بائت · لم يختفِ
    > ⇒ زرٌّ ممسوك»**) **كلاهما افترض أن إخفاءنا يقع والسؤال «متى»** — ⛔ **فسُحبا
    > معاً**. **والمقيس:** الشريط اختفى بعد 3–4 ثوانٍ، **ثمّ اختفى بالمدّة نفسها
    > والإضافة مطفأة** ⇒ **الاختفاء إخفاءُ يوتيوب التلقائيّ، وميزتُنا لا تعمل
    > إطلاقاً في تلك الحال** — لا «تعمل بمهلةٍ طويلة».
    > ⇒ ⭐ **فأثرُنا وأثرُ المضيف متطابقان في العين، وقراءةُ الأثر لا تقول من فعل.**
    > **والمالك قاس سببَنا لا الأثر: أطفأ الإضافة وقارن** — **وهي القاعدة نفسها
    > مُطبَّقةً على أهون آلة، ولم نطلبها منه.**

    > ⭐ **وعملت في أوّل موضعٍ بعد تسميتها (2026-08-04، #91):** أوّل صياغةٍ لشاهد
    > القياس قرأت `.ytp-title` نفسه فوجدته `display:none` **وحسبته أثرَنا** —
    > **ويوتيوب يُخفيه بنفسه خارج ملء الشاشة**. ⇒ **فسقط الشاهد السالب بحقّ**،
    > وصار القياس على **عنصرٍ من صنعنا** يحمل الصنف: `display:none` بالمفتاح
    > مُشغَّلاً، و`block` بصنفٍ محايد. **قياسُ الأثر لا يُميّز فاعلَه، فيُقاس سببُنا.**

58. **الاجتهاد في «ما يلزم تشغيله» هو ما لُدغنا منه — فيُشغَّل الأربعة دائماً**
    (قرار المالك 2026-08-03، تثبيتاً للنصّ الثالث في قرار 32).
    **شُغِّلت الأربع على تغييرٍ توثيقيّ محض وأقرّ المالك ذلك** — **لأن الاستثناء
    الوحيد الذي اجتُهد فيه كان خاطئاً**: «لم أمسّ `options.*` فالفحص غير لازم»،
    والصفحة تحمل `ReferenceError` وقتها. ⇒ **وكلفةُ تشغيلٍ زائد دقائق، وكلفةُ
    استثناءٍ خاطئ جلسةُ مالك.**

## 6. بروتوكول العمل

1. **مهمة واحدة في كل دورة** — لا تجمع بندين إلا إذا طلب المستخدم ذلك صراحةً.
2. **أقل عدد أسطر** يحقّق الغرض.
3. **اختبار آلي حيثما أمكن** تحت `tools/`، ويجب أن **يفشل على الكود السابق**.
   وثِّق كم تأكيداً يفشل: #26 يفشل بـ16، و#47 يفشل بـ13.
4. **كومِت مستقل برقم البند**: `[12] وصف بالعربي، فعل ماضٍ، أقل من 60 حرفاً`.
5. **خطوات اختبار يدوي للمستخدم**، مرقّمة وواضحة. ومنصّتا §8 و§9 **مُلزِمتان**
   حسب ما يمسّه البند (قرار المالك 17).
6. **ثم قف وانتظر تأكيده** قبل البند التالي.
7. **فرعنة:** لا تفرّع عملاً من فرع غير مدموج، ولا تترك فرع توثيق معلّقاً بعد
   التحقق منه — يُدمج فوراً (قرار المالك 18).
8. **كل شرط «إن كان كذا» يُقاس على الشجرة والسجل قبل تنفيذه**، وإن خالف الواقع
   فأبلِغ ولا تنفّذ (قرار المالك 16).

> ⚠️ **المستخدم هو من يختبر في المتصفح — أنت لا تستطيع.** لا تدّعِ أنك جرّبت في
> كروم. اكتب له خطوات مرقّمة تشمل مسار النجاح **ومسار عدم الانحدار**، وإن كان
> الاختبار يتطلب افتعال فشل فأعطه سكربت كونسول جاهزاً للصق.
>
> ولا تخترع تعديلاً لبند سليم. إن فحصت شيئاً ووجدته صحيحاً، **قل ذلك** وأضف
> اختبار انحدار بدل تغيير الكود. وإن ناقض القياسُ فرضيةَ المستخدم فقل ذلك
> بالأرقام قبل أن تنفّذ — حدث مراراً وكان الصواب مع القياس.
>
> **وإن لم تُعِد إنتاج عطب بنفسك فقل ذلك صريحاً ولا تُخمّن سبباً.**
>
> **ولا تكتب في التوثيق أن اختباراً «مُعلَّق» وقد نُفِّذ، ولا العكس.** بقي §8
> يقول إن اختبار #56 و#24 لم يُنفَّذ بعد أن نفّذه المالك وأكّده — فجوة تسجيل
> لا فجوة تنفيذ، وقد صُحّحت. سجّل كل تأكيد **بتاريخه** لحظة وقوعه.

---
