# HANDOFF — تعليمات استئناف العمل

> هذا الملف مكتوب لمن يفتح المشروع في جلسة جديدة **بلا أي معرفة سابقة** بما جرى.
> اقرأه كاملاً قبل أن تكتب سطراً واحداً. المرجع التفصيلي للأخطاء هو `AUDIT.md`.

---

## 0. مُعلَّق على `main` — اقرأه أولاً

| # | الحالة | ما هو |
|---|---|---|
| **#59** | ✅ **كومِتان مُنفَّذان ومقيسان · بانتظار اختبارك اليدوي** | فيديو ابن مباشر لـ`body` بلا سلف يشبه مشغّلاً ⇒ الحكم القاطع في #58 **لا يمسّه بنيوياً** فيسقط إلى السكور، وهناك يُكبَّر **`<body>`** نفسه. **يشترك مع #58 في الجذر — هشاشة السكور — وهو الباقي بعده لا بديلاً عنه.** أي إصلاح له يمسّ السكور نفسه، وهو ما رفضه المالك مبدئياً في #58، فيحتاج قراراً مستقلاً |

> ✅ **كومِت أ:** `body` و`documentElement` مستثنيان من مرشّحي السكور — بنفس مبرّر
> استثنائهما من الحكم القاطع في #58. **السكور لم يُعدَّل بحرف** ولا فاصل تعادل
> مُضاف. قِيس: البنية أ ⇒ **`VIDEO` في 15 من 15 تركيبة**، وعدّ البوابة **4 ⇒ 3**،
> وكل البنيات العشر حتمية. وتثبيت القسم [8] وسطر `KNOWN-DEFECTS.md` **حُدِّثا في
> الكومِت نفسه** — أُزيل تثبيت العطب وحلّ محلّه فحص موجب يحرس الإصلاح.
>
> ⚠️ **تغطية القياس، بلا تجميل:** دخول المسار الاحتياطي **صفر في 15 قياساً**
> (10 بنيات محلية + 5 مقاطع على مواقع حقيقية). **لكن ثلاثة مواضع عجز عنها
> `headless`**: `vimeo.com` و`twitch.tv` (قِيسا عبر **روابط المشغّل** لا الموقع)
> و**`x.com` بلا قياس إطلاقاً**. **قاعدة عامة: قياس «موقع حقيقي» في headless لا
> يشمل ما يحتاج تفاعلاً أو تسجيل دخول، فلا يُعمَّم صفرُه على الموقع الكامل** —
> و«لم أقس» ليست «قِستُ فوجدت صفراً». التفصيل في `AUDIT.md` §4ج.
>
> ✅ **كومِت ب:** المسار الاحتياطي `video.parentElement || video` ⇒ **`video`**.
> الاستثناء وحده كان يضيّق #59 ولا يغلقه: أبو الفيديو في البنية أ هو `body` نفسه،
> فطريقه الأخير يبقى مفتوحاً. ولا يُدخَل هذا المسار إلا وقد **رُفض كل المرشّحين أو
> كانت مستطيلاتهم صفرية** — تخطيط منهار لا شريط تحكّم فيه ليُخسر. قِيس قبل التغيير
> أنه **خامد على كل ما قِسناه**: دخول المسار **صفر** في 10 بنيات محلية و**7** مقاطع
> على مواقع حقيقية (d.tube · يوتيوب مشاهدةً ومضمَّناً · فيميو · تويتش · x.com
> تغريدةً وتايم-لايناً). اختبار: `test-container-choice.js` [9] — 21 تأكيداً تشترط
> أن الاحتياطي **لا يُرجع `body` ولا `documentElement` ولا أب الفيديو** في أي بنية.
>
> **صفحة اختبار يدوي:** `tools/manual-59-body.html` — تُعيد إنتاج البنية أ (فيديو
> ابن `body` · صفحة قصيرة · زر واحد) بلا شبكة.

**#58 مغلق ومدموج** — انظر §2ب أدناه. ولا شيء آخر مُعلَّقاً: كل بنود الموجة 4
مؤكَّدة يدوياً من المالك (§8)، و#58 كذلك.

---

## 1. أين نقف الآن بالضبط

| | |
|---|---|
| **الفرع** | **`main` على `2.11.1`، الشجرة نظيفة، لا فرع نشط** |
| **آخر كومِتات `main`** | `c889769` رفع النسخة إلى 2.11.1 · `0ef6003` دمج #58 · `c2d36f0` رفع 2.11.0 · `61e6a89` دمج الموجة 4 |
| **الموجة 4** | ✅ **مكتملة ومدموجة** — 20 كومِتاً، والفرع `fix/audit-wave4` حُذف محلياً وعلى `origin` بعد التحقق |
| **الوسوم** | **لا وسوم في المستودع إطلاقاً** — لا محلياً ولا على `origin`. لا نمط قائم، فلا تُنشئ نمطاً جديداً. ولا وسم للموجات، هذا هو البروتوكول |
| **#58** | ✅ **مغلق ومدموج** — كومِتان مستقلان `ed48128` و`4a07651`، ومؤكَّد يدوياً |
| **البند التالي مباشرةً** | **#59** — تشخيص فقط، ثم الموجة 5 |
| **الاختبارات** | **949 تأكيداً** في **32 ملفاً**، كلها ناجحة |

### الموجات

| الموجة | الحالة | النسخة |
|---|---|---|
| إصلاح `S1` (معزّز الصوت) | ✅ مدموجة `2a8f59c` | 2.7.0 |
| الموجة 1 | ✅ مكتملة ومدموجة | 2.9.0 |
| الموجة 2 | ✅ البند الوحيد فيها (#6) مدموج `5e660a6` | 2.9.0 |
| الموجة 3 | ✅ مكتملة ومدموجة — 7، 9، 11، 12، 16، 17، 18، 20، 22، 33 · واكتُشف فيها 46، 47، 48 | 2.10.0 |
| **الموجة 4** | ✅ **مكتملة ومدموجة** — 13، 13ب، 14، 19، 21، 24، 26، 47، 49–56 · رُفض 25 · سُجِّل 57 | **2.11.0** |
| **#58** | ✅ **مغلق ومدموج** — `ed48128` + `4a07651` · دمج `0ef6003` | **2.11.1** |
| **#59** | 🟡 **البند التالي** — مُسجَّل، تشخيص فقط، يحتاج قراراً مستقلاً (يمسّ السكور) | — |
| الموجة 5 | ⬜ لم تبدأ | — |

**بروتوكول إغلاق أي موجة** (اتُّبع حرفياً في الموجتين 3 و4): ادفع الفرع، ادمجه في
`main` بـ `--no-ff --no-edit`، ارفع النسخة في `manifest.json` **بكومِت مستقل**، ادفع
`main`، تحقق بـ `git log main..<الفرع>` أنه صفر، ثم احذف الفرع بـ `-d` محلياً
وعلى `origin`. **بلا وسم.**

**قاعدة رفع النسخة — مستقرة، طبّقها بلا سؤال:** ميزة ⇒ رفع **minor**، إصلاح ⇒ رفع
**patch**. الموجة 4 أدخلت ميزة (#51) و service worker جديداً ⇒ `2.10.0 ← 2.11.0`.
و#58 إصلاح محض بلا ميزة ⇒ `2.11.0 ← 2.11.1`.

---

## 2. الموجة 4 بنداً بنداً — بكومِتاتها

| البند | الكومِت | ماذا فعل |
|---|---|---|
| — | `01563ea` | سجّل رفض #25 وتضييق نطاق #26 (قرارا المالك 9 و10) |
| **#13** | `553558b` | البدء صار **قراءة تخزين واحدة** بدل إحدى عشرة، تُمرَّر لكل مُحمِّل عبر معامل `pre` اختياري |
| **#49** | `d0e7c46` | حجم خط الترجمة نسبيّ لمقاس المشغّل بوحدات استعلام الحاوية + `clamp` |
| **#50** | `21cb8f0` | حارس بنيوي: الحجم النسبي داخل `@container` فقط، والمطلق احتياطي دائم |
| **#51** | `04a0a85` | مفتاح «إخفاء الترجمة في معاينات الصفحة الرئيسية»، مُفعَّل افتراضياً |
| **#52** | `281f4ad` | الإضافة تتجاوز «لون النافذة» في يوتيوب بدل أن يظهر تحت صندوقنا |
| **#53** | `da3a179` + `d7761f8` | صندوق ترجمة **واحد** بنسب يوتيوب، بدل ثلاث طبقات متراكمة |
| **#54** | `2c29fc7` | «شفافية» ⇒ «تعتيم» في العناوين الأربعة — تسمية فقط، صفر هجرة |
| **#13ب** | `0b15311` | الإطار بلا فيديو لا يبدأ، ويستيقظ بأحداث الوسائط لا بمراقب |
| **#14** | `81e17b5` | مُطبِّق واحد لقناتَي تحديث الإعدادات، بلا تكرار العمل |
| **#21** | `b406244` | مراقب الترجمة يُنشأ عند التفعيل وحده ويُفصل عند الإطفاء، ونطاقه `body` |
| **#19** | `7daeee2` + `9afbb36` | استطلاع جودة واحد بلا إعلانات وبسقف صريح + سطر حالة عند تعذّر الجودة |
| **#55** | `6b0c28d` | فحص: المراقب **ليس زائداً** — توثيق فقط، لا تغيير كود |
| **#56** | `472f4cb` | الـ popup يحسب الحظر والتفعيل بنفسه لا من إطار |
| **#24** | `04914f3` | لا بثّ في مسار التعزيز، والمُنزلق يُعطَّل برسالة بدل أن يوهم |
| — | `d165ae7` | تحديث HANDOFF قبل الإغلاق |
| **#26** | `fe9e58d` | **service worker للهجرة وحدها** عند التثبيت والتحديث، بأضيق نطاق |
| **#57** | `3d66085` | تسجيل بند الموجة 5 في `AUDIT.md` — **تسجيل فقط، بلا تنفيذ** |
| **#47** | `9200d61` | الـ overlay يصعد إلى الطبقة العليا بـ `popover="manual"` فوق `<video>` مكبَّر |
| — | `61e6a89` · `c2d36f0` | الدمج ورفع النسخة إلى `2.11.0` |

**#25 مرفوض نهائياً** بقرار المالك — لا كومِت له ولا يُعاد فتحه.

---

## 2ب. البند #58 — مغلق، كومِتان مستقلان

**الموقع الذي رُئي عليه (قرار 19):** `https://d.tube/watch/5MxdC3ajEpBwgcDCsrHRd5`.
**سببان مستقلان لا سبب واحد**، ولكل واحد كومِت قابل للإرجاع وحده:

| الكومِت | السبب | ما فعل | القياس |
|---|---|---|---|
| **`ed48128`** | (1) العنصر الخطأ يُكبَّر | حكم قاطع يسبق السكور: **أقرب** سلف يشبه مشغّلاً ويملؤه الفيديو (≥ `VZ_FILL_RATIO`) هو الحاوية، والمشي يتوقّف عنده — «الأخصّ يفوز» نفسه في #48 | `d.tube`: **63% ⇒ 100%** · العنصر: `DIV.md:container` ⇒ `DIV.dtube-player-wrapper` |
| **`4a07651`** | (2) الفيديو غير نسبيّ داخل حاوية صحيحة | بوابة قياس عند `fullscreenchange`: سمتان + ورقة أنماط **فقط** إن كان الفيديو لا يملأ ما كبّرناه، بنفس الثابت لا برقم ثانٍ | البنيات ج·د·ز: **48% ⇒ 100%** · البوابة: **4 موسومة · 6 مرفوضة · صفر سمة باقية** |

الدمج `0ef6003`، ورفع النسخة `c889769` (`2.11.0 ← 2.11.1` — إصلاح لا ميزة).

**رُفض تغيير النسبة المثالية 1.15 لسبب مبدئي لا حسابي:** إصلاح هامشه 0.1037 نقطة
هشاشة لا حكم. **المعادلة الغامضة لا تُعدَّل، بل يُسبَق إليها بحكم قاطع.**

> ⚠️ **ادّعاء مسحوب (قرار 21):** قيل إن تغيير `1.15` «لا يمسّ يوتيوب ولا X، فهما
> يخرجان قبل السكور». **`x.com` يصل إلى السكور فعلاً** — الفرع `score` في التغريدة
> وفي التايم-لاين (خطّ الأساس في `AUDIT.md` §4ج تحت #59). لم يدخل الادّعاء أي ملف
> فلا شيء حُذف، لكنه مسحوب صراحةً. **وتقييم خطر الخيار الأول كان على افتراض غير
> مقيس، فنطاق أثره أوسع مما قُدِّر.** وهذا **شاهد ثالث** على صواب رفض تعديل
> المعادلة: رُفض مبدئياً فتبيّن أنه صواب **لأسباب لم نعرفها كلها وقتها**.
>
> ⚠️ **تبرير مُصحَّح:** كان القرار على **شاهدين** — الفارق المقيس 0.1037، **وانقلاب**
> الحالة أ. **الشاهد الثاني سُحب** (أثر توقيت في منصّة القياس). **القرار يصمد على
> الشاهد الباقي**، والتبرير من الآن: **هشاشة سكور ناعم مقيسة لا انقلاب مرصود.**

**شرط قبول دائم في كل ما يمسّ اختيار الحاوية: الحتمية.** كل بنية تُشغَّل على
**مقاسي إطار عرض** ويُشترط أن يكون العنصر المختار واحداً —
`tools/test-container-choice.js` و`tools/repro-58-fullscreen.mjs`.
النتيجة الحالية: **10 من 10 حتمية**.

> ⚠️ **تصحيح مقيس (2026-07-30):** كان مسجَّلاً أن الحالة أ «تنقلب بين مقاسَي إطار
> عرض بفارق ≈0.1». **قِيس فتبيّن أن الانقلاب كان أثر توقيت في منصّة القياس نفسها**
> (450ms لم تكفِ بعد `textContent=""` فبقي `body` بارتفاع 0 لحظةً فرُفض). صار
> الانتظار 1100ms وكل البنيات حتمية. **و#59 قائم كما هو** لكن وصفه صار: `BODY`
> يفوز **حتمياً**، والمميِّز **طول الصفحة** لا عرض إطار العرض.

> ⚠️ **حدّ معروف — مُصرَّح به و«غير مُختبَر»:** حاوية تحمل واجهة **في التخطيط**
> (لائحة تشغيل أو شريط جانبي **داخل** العنصر المكبَّر) — تمديد الفيديو إلى
> `100%×100%` يطمس تخطيطها. والبوابة **لا تحمي منه** لأن الفيديو فيها فعلاً لا
> يملأ الحاوية. الأزرار المطلقة الموضع لا تتأثر.

---

## 3. البنود المكتشفة أثناء العمل — 46 إلى 57

لم تكن في الفحص الأول. **ثلاثة منها انحدارات أدخلناها بأنفسنا**، والبقية عيوب
قديمة كشفها العمل. التمييز مهم: الانحدار يعني أن اختباراتنا لم تغطِّ شيئاً.

| # | المصدر | ماذا كان | الحالة |
|---|---|---|---|
| 46 | عطب قديم (منذ 2.3.0) | حارس `host.contains` يترك الـ overlay عالقاً بعد أي دورة ملء شاشة — إلى نهاية عمر الصفحة | ✅ `2ef0b3a` |
| 47 | قيد متصفح | لا يُرسم overlay فوق عنصر ملء شاشة هو `<video>` نفسه | ✅ `9200d61` — **تحصين**، والقياس الميداني يبقى قائماً |
| 48 | عطب قديم | لا أولوية بين ربط المربع والقواعد العامة، **وتنفيذ مزدوج معه** | ✅ `c9d7bac` |
| 49 | عطب قديم (منذ الكومِت الأول) | حجم خط الترجمة مطلق بالبكسل ⇒ يغرق المعاينات الصغيرة | ✅ `d0e7c46` |
| 50 | **⚠️ انحدار من #49** | `cqw` بلا حاوية لا تفشل بل ترجع لإطار العرض: **32.66px** بدل 22 — أكبر مما كان، وبصمت | ✅ `21cb8f0` |
| 51 | ميزة بطلب المالك | مفتاح إخفاء الترجمة في المعاينات | ✅ `04a0a85` |
| 52 | عطب قديم | «لون النافذة» في يوتيوب يظهر تحت صندوقنا ويبقى بعد تعطيل الإضافة | ✅ `281f4ad` |
| 53 | عطب قديم (منذ 2.3.0) | الخلفية تُطلى على **ثلاث طبقات متداخلة** ⇒ مُنزلق التعتيم يكذب منذ 2.3.0 | ✅ `da3a179` |
| 54 | تسمية خاطئة | «شفافية» تصف عكس ما يفعله المُنزلق — في القسمين معاً، بلا تناقض بينهما | ✅ `2c29fc7` |
| 55 | فحص | هل المراقب زائد؟ **لا** — أُغلق بلا تغيير كود | ✅ `6b0c28d` |
| 56 | **⚠️ انحدار من #13ب** | الإطار النائم يردّ على `GVZ_STATUS` بقيم افتراضية ⇒ الـ popup يقول «متوقفة» والإضافة شغّالة | ✅ `472f4cb` |
| 57 | هشاشة بنيوية | مسار الاستيراد يعتمد على `location.reload()` لصحّة الهجرة — أي تغيير يُسقط إعادة التحميل يُسقط الهجرة **بصمت** | ⬜ **الموجة 5** |

**ثلاثة دروس منهجية:**

- **#50:** لم يكن كافياً أن نُصلح؛ كان لا بدّ من سؤال «وماذا لو لم تُنشأ الحاوية؟».
  الفشل الصامت الذي يعود **أسوأ** مما كان هو أخطر ما في تغيير آلية. طُبِّق الدرس
  في #47: فشل `showPopover` يُزيل السمة فوراً، فأسوأ الحالات = سلوك اليوم لا أسوأ.
- **#56:** `test-frame-wake.js` غطّى **الإيقاظ** ولم يغطِّ **ما يقوله النائم**،
  فأفلت الانحدار كاملاً. القسم [6] فيه أُضيف لسدّ هذي الفجوة تحديداً.
- **#57:** الصحّة المعلَّقة بأثر جانبي (`location.reload()`) ليست صحّة. سجّله
  المالك بنداً مستقلاً بدل أن يبقى ضمنياً.

---

## 4. قرارات مالك المشروع — نهائية، لا تُعاد ولا يُسأل عنها

1. **التخزين يبقى في `chrome.storage.sync`** مع تجزئة `siteProfiles` إلى مفتاح
   لكل نطاق (`sp:<domain>`). **لا انتقال إلى `storage.local`** — نوقش ورُفض لأن
   `local` لا يُزامَن بين الأجهزة.
2. **`gridAppearance` مُنفَّذة لا محذوفة** — الوعد في واجهة الإعدادات صار صادقاً.
3. **المُعدِّلات محترمة في كل مسارات الاختصارات** — `Shift+ArrowRight` توقيع
   مستقل عن `ArrowRight`.
4. **#20:** تحويل Shorts **يُربط بالتفعيل العام** — إيقاف الإضافة يوقف كل شيء
   بلا استثناء.
5. **#18:** يُستثنى زرّا الترجمة والإعدادات من Clean Player **تلقائياً** عند
   تفعيل أتمتة اللغة، بلا سؤال المستخدم، مع سطر توضيحي في الواجهة.
6. **لا حذف تلقائي لأي بيانات مستخدم في أي مسار، أبداً.** كل حذف خلف تأكيد صريح.
7. **لا تنبيه دائم في الواجهة، والنجاح صامت.** أي سطر حالة لا يظهر إلا عند خلل
   فعلي — سطر يظهر دائماً يتعوّد المستخدم على تجاهله (درس التنبيه الأزرق في
   `S1`). طُبِّق في سطر تعذّر جودة يوتيوب: يظهر عند فرق فعلي فقط، ويذكر
   **الرقمين** (المطلوب والمُطبَّق)، ولا يظهر على الجودة التلقائية ولا أثناء
   إعلان ولا خارج يوتيوب.
8. **#48 — الأخصّ يفوز دائماً:** `ربط المربع > قاعدة الموقع > القاعدة العامة`،
   ومتى فاز الأخصّ **لا يُنفَّذ الأعمّ إطلاقاً، لا قبله ولا بعده**. المبرّر أن
   المربع موضع محدد اختاره المستخدم عمداً فلا تطمسه قاعدة تعمّ الشاشة كلها.
   المصدر الواحد: `zoneClickBinding` للفأرة و`zoneKeyBinding` للمفاتيح — أي مسار
   إدخال جديد يستشيرهما، ولا تُكرَّر قواعد الأولوية في مكان آخر.
9. **#25 مرفوض نهائياً — لا يُعاد فتحه:** حذف `tabs` و`activeTab` مكسبه صفر،
   لأن تحذير التثبيت مصدره `<all_urls>` نفسه والإضافة شخصية لا تُنشر في المتجر.
   البديل الصحيح لو تغيّر ذلك هو نقل `<all_urls>` إلى `optional_host_permissions`
   بقرار منفصل ورقم جديد.
10. **#26 بأضيق نطاق:** الـ service worker **لا يفعل شيئاً سوى الهجرة** في
    `onInstalled`. لا مستمعات أخرى ولا نقل أي منطق قائم إليه. غياب الـ service
    worker قرار معماري متعمَّد، وهذا استثناؤه الوحيد لأن الهجرة كانت لا تعمل إلا
    لمن يفتح صفحة الإعدادات. المبرّر مكتوب في تعليق أعلى `background.js`.
    **وهجرة الـ worker هي دالة الهجرة نفسها لا نسخة ثانية منها.**
11. **الـ popup لا يسأل سكربت المحتوى عن حقيقة يملكها التخزين أو رابط التبويب.**
    انظر التفصيل في §5.
12. **#47 نُفِّذ تحصيناً، والقياس الميداني يبقى قائماً:** قاس المالك بنفسه
    (2026-07-29) دخول ملء الشاشة بأمر الإضافة على **ثمانية مواقع** — يوتيوب · X ·
    إنستغرام · تيك توك · ستيم · فيميو · Kick · تويتش — والشبكة ظهرت في الثمانية
    كلها، أي أن السقوط إلى `<video>` في `pickFullscreenContainer` لم يقع في أيٍّ
    منها. **فشرط قبول #47 كان ألا يتغيّر المسار الحالي بشيء**، لا أن يُصلح عطباً
    يومياً. من يمسّ هذا المسار لاحقاً يحمل العبء نفسه.
13. **قاعدة رفع النسخة مستقرة:** ميزة ⇒ minor، إصلاح ⇒ patch. تُطبَّق بلا سؤال.
14. **لا وسوم في هذا المستودع — النقاش مُغلق.** لا وسم للموجات، ولا نمط وسم قائم
    يُتبع، ولن تُنشأ وسوم.
15. **#58 — نطاق الإصلاح مثبَّت قبل التنفيذ:** مسار `requestFullscreen` وحده،
    **بلا لمس مسار الزر الأصلي إطلاقاً**، وسمة خاصة على العنصر الذي كبّرناه نحن،
    وكل قاعدة CSS محصورة بها، وتُزال عند الخروج بلا أثر باقٍ. التفصيل في §0.
16. **كل شرط بصيغة «إن كان كذا» يُقاس على الشجرة والسجل قبل تنفيذه.** إن خالف
    الواقعَ فأبلِغ المالك **ولا تنفّذه**. مصدر القاعدة واقعة حقيقية: طُلب نقل
    خطوات اختبار #56 و#24 إلى قسم «مُعلَّق» **إن لم تكن نُفِّذت** — وكانت قد
    نُفِّذت وأُكِّدت، فنقلها كان سيُنشئ **معلَّقاً وهمياً**. التنفيذ الأعمى لشرط
    معلَّق على واقع هو خطأ، لا طاعة.
17. **منصّتا الانحدار مُلزِمتان، لا اختياريتان:** كل بند يمسّ حالة الـ popup
    (`GVZ_STATUS` · `blockedHosts` · `findVideoFrameId`) **يُشغِّل منصّة §8 كاملة**،
    وكل بند يمسّ الطبقة العليا أو ملء الشاشة أو اكتشاف الفيديو **يُشغِّل منصّة §9
    كاملة**. الخطوات التسع في §8 ليست قائمة مُعلَّقة بل منصّة دائمة.
18. **لا يُفرَّع عمل من فرع غير مدموج، ولا يُترك فرع توثيق معلّقاً بعد التحقق منه
    — يُدمج فوراً.** هذه القاعدة هي التي تمنع تعارض `AUDIT.md` من التكرار: فرعان
    يكتبان في نفس جدول التقرير يتعارضان حتماً.
21. **الرقم المسحوب يُسحب من كل موضع ذُكر فيه، في الكومِت الذي يسحبه نفسه.**
    **رقم ميّت في ملف ثانٍ أخطر من رقم خاطئ في ملف واحد** — لأن الأول يُقرأ حجّةً
    وقد سقط سنده. ويُترك **مشطوباً بسببه** لا محذوفاً صامتاً، كي يعرف من يقرأ
    لاحقاً أنه **سُحب** لا أنه لم يُكتب. أول تطبيق: انقلاب الحالة أ (2026-07-30) —
    سُحب من `AUDIT.md` و`HANDOFF.md` و`KNOWN-DEFECTS.md` و`content.js` و
    `test-container-choice.js` و`repro-58-fullscreen.mjs` في كومِت واحد.
    **ولا تُبقَ في السجل حجّة لا تسندها قياسة:** قرار #58 كان على شاهدين فسقط
    أحدهما، فصُحّح تبريره إلى «هشاشة سكور ناعم مقيسة» بدل «انقلاب مرصود».
22. **كل قياس في أي منصّة يشترط استقرار التخطيط ومستطيلات غير صفرية قبل أن
    يُقرأ.** درس `450ms ⇒ 1100ms`: **أثر توقيت ارتقى إلى «حقيقة منشورة» لأن
    القياس قرأ قبل أن يستقرّ الشيء المقيس** — `body` بعد `textContent=""` يبقى
    بارتفاع 0 لحظةً، فيُرفض بحارس المستطيل الصفري ويفوز مرشّح آخر. من يكتب منصّة
    قياس ينتظر التخطيط **ويتحقّق أن المستطيل غير صفري** قبل أن يبني على الرقم.
20. **العطب المعروف يُثبَّت في مجموعة الاختبارات ويُبرهَن في منصّة القياس،
    ومجموعة الاختبارات تبقى خضراء دائماً.** قيمة الـ949 تأكيداً كلها في أن الأحمر
    يعني «كسرتَ شيئاً **الآن**» لا «شيء مكسور منذ أسبوع» — **مجموعة حمراء دائماً
    مجموعة ميتة**. وكل تثبيت عطب يحمل فوقه نصّاً صريحاً: «فشل هذا التثبيت يعني أن
    `<البند>` أُصلح، فحدّثه ولا تُصلح الاختبار». والعطب المعروفة كلها في موضع
    **واحد** يُقرأ قبل تشغيل المنصّة: `tools/KNOWN-DEFECTS.md`.
19. **كل بند يُسجَّل في `AUDIT.md` من الآن يحمل الرابط الذي رُئي عليه. بند بلا
    موقع مُسجَّل لا يُقبل.** سبب القرار أن المالك رأى عطب #58 على أكثر من موقع
    ولم يسجّل أسماءها، فضاع نصف الدليل. **وقبل الإبلاغ عن أي عطب ملء شاشة يُلصق
    `tools/report-fullscreen-bug.js` في كونسول الصفحة** — يطبع سطراً واحداً
    قابلاً للنسخ فيه الرابط والحاوية المختارة ومستطيلها ومستطيل الفيديو والنسبة
    وحكم البوابة. وهو تشخيصيّ **لا يُشحن**، ونسخته من منطق `pickFullscreenContainer`
    محروسة بـ `tools/test-fs-report-sync.js`.

---

## 5. ثوابت معمارية لا تُكسر

**الكتل المقترنة حرفياً بين `storage.js` و `content.js`**
ثلاث كتل — `baseDomain` و `normalizeKeyCombo` و `gridAppearance` — مكرّرة **نصّاً
متطابقاً** بين الملفين، محدّدة بعلامات `// ---- BEGIN <name> ----` و `END`.
السبب: `content.js` سكربت محتوى ولا يستطيع تحميل `storage.js` بلا حقنه في كل
إطار. **عدّل النسختين معاً دائماً** — `tools/test-migration.js` يقارنهما نصّياً
ويفشل عند أي انحراف. **ولا تُضِف دالة جديدة داخل كتلة مقترنة** — أُضيفت
`isHostBlocked` خطأً داخل كتلة `normalizeKeyCombo` فأسقط الاختبار البناء فوراً؛
مكانها الصحيح **خارج** أي كتلة.

**مدخل هجرة واحد: `migrateAll` في `storage.js`** (البند #26)
يجمع `migrateSiteProfiles` و`migrateZoneActions`. يستدعيه `background.js` عند
`onInstalled` وتستدعيه صفحة الإعدادات، **وأيّهما جاء ثانياً وجد العمل منتهياً فلم
يكتب شيئاً**. وتحويل `wheel.map ⇒ wheel.actions` — ومعه `makeId` و
`normalizeActionArray` و`parseRuntimeAction` — يعيش في `storage.js` **نسخة واحدة**
لا في `options.js`. **لا تُعِد تعريف أيٍّ منها في `options.js`.**
والـ service worker **كلاسيكي بـ `importScripts`، بلا `"type": "module"`**: تحويل
`storage.js` إلى وحدة ES يكسر تحميلها كسكربت عادي في `popup.html` و`options.html`،
**ويُدخل `export` داخل الكتل المقترنة نصّياً فيُسقط `tools/test-migration.js`**.

**`background.js` لا يفعل شيئاً سوى الهجرة**
مستمع واحد `chrome.runtime.onInstalled`. لا `chrome.runtime.onUpdated` في الـ API
أصلاً — التحديث هو `onInstalled` بـ `reason === "update"`. أي مستمع ثانٍ أو أي
منطق منقول إليه يخالف قرار المالك 10، و`tools/test-sw-migration.js` يعدّ
`addListener` ويفشل إن زاد.

**`safeSyncSet` هو مسار الكتابة الوحيد إلى `storage.sync`**
يفحص الحدود الثلاثة (8KB للعنصر · 100KB للمجموع · 512 عنصراً) قبل الكتابة، ثم
يلتقط الرفض الحقيقي ويترجمه لعربية. **الاستثناء الوحيد: `importAllSettings`** —
لأن حارس الحجم يقيس التخزين الحالي بينما الاستيراد يمسحه قبل الكتابة. شبكة
الأمان هناك هي **لقطة الاسترجاع** لا الحارس.

**الهجرة لا تطمس شظية حيّة أبداً**
`migrateSiteProfiles` تتخطّى أي `sp:<domain>` موجود بمحتوى مختلف، وتُدرجه في
`conflicts`، وتُبقي المدخل القديم. صفحة الإعدادات تعرض شريطاً يسرد المتعارضات
مع زر حذف خلف تأكيد صريح. **ولا كتابة حين لا بيانات قديمة**: `migrateZoneActionsInto`
تُرجع `false` فتُلغى الكتابة كلياً — تثبيت جديد ⇒ صفر كتابة.

**«لا واجهة تقول نجح والكتابة فشلت»**
`تم الحفظ ✅` لا تظهر إلا بعد نجاح فعلي. كل مسار حفظ يُرجع نتيجة، والفشل يظهر
كـ toast أحمر في الإعدادات أو رسالة حمراء في صندوق حالة الـ popup.

**الـ popup لا يسأل سكربت المحتوى عن حقيقة يملكها التخزين أو رابط التبويب**
(قاعدة دائمة، البند #56). «هل الإضافة مفعّلة» إعداد مخزَّن، و«هل الموقع محظور»
دالة في (رابط التبويب، `blockedHosts`) — يُحسبان في الـ popup بـ `baseDomain` و
`isHostBlocked` من `storage.js`، **ولا نسخة ثالثة**. ولا يُسأل الإطار إلا عمّا
يملكه وحده: هل فيه فيديو، وهل حُقن السكربت. والإطار النائم يردّ `not-started`
و**لا يستيقظ لرسالة أبداً** — الإيقاظ بالرسائل يُبطل #13ب كلّه. و«لا فيديو في
هذه الصفحة» رسالة **مستقلة** عن «الإضافة متوقفة»؛ لا تُخلطا.

**لا بثّ `sendMessage` بلا `frameId` في أي مسار** (البند #24): البثّ يجعل كل
إطار يبني `AudioContext` خاصاً به ويجعل إطاراً عشوائياً يردّ على
`GET_VOLUME_BOOST`. حلّ الإطار يمرّ بـ `findVideoFrameId` وحدها — **مصدر واحد**
لمسار الحالة ومسار التعزيز — وبلا إطار محلول يُرفض الإرسال ويُعطَّل المُنزلق
برسالة، لا أن يتحرك بلا أثر.

**الـ overlay والطبقة العليا** (البند #47)
`fullscreenElementFor` هي **المصدر الواحد** لعنصر ملء الشاشة، وتقرأ من جذر الظل
لا من المستند لأن `document.fullscreenElement` **يُعاد استهدافه** إلى مضيف الظل
(#46). و`setOverlayTopLayer` ترفع الـ overlay بـ **`popover="manual"`** في حالة
واحدة فقط: أن يكون عنصر ملء الشاشة هو `<video>` نفسه. **لا تُغيّرها إلى `auto`**:
الـ auto يُغلق بـ `Esc` وبالنقر خارجه **ويطرد أي popover آخر مفتوح على الصفحة**.
والسمة لا تُضاف في أي حالة أخرى، فقواعد `[popover]` الافتراضية لا تنطبق و**المسار
الحالي لا يتغيّر بشيء** — `tools/test-top-layer.js` يحرسه بعدّ الاستدعاءات:
**صفر** في المسار العادي وفي ملء الشاشة بحاوية أعلى.
وممرّ الرفع **واحد**: من `attachOverlayToHost` وحدها، لأن نقل العنصر في الـ DOM
يُغلق أي popover مفتوح فيلزم إعادة فتحه بعد كل نقلة.

**صندوق الترجمة: حامل واحد، بنسب يوتيوب** (البند #53). يوتيوب يطلي صندوقه على
`.ytp-caption-segment` بحشو `0 .25em` — قِيس حيّاً على خمسة مقاسات وهو `em` في
ورقته فثابت بالبناء. نحن نطلي **حاملاً واحداً** هو `.caption-window` بنفس النسبة
(0.25 أفقياً · 0 رأسياً)، ونُصفّر خلفية وحشو كل ما بداخلها **صراحةً** — وهذا
يشمل طمس «لون النافذة» عند يوتيوب، فالنافذة صارت حاملنا لا شفافة (هذا **عكس** ما
فعله #52، وقد نسخه #53). **لا تُعِد طلاء أي مستوى داخلي**: محدّداتنا العريضة
تطابق ثلاثة مستويات متداخلة، وطلاؤها معاً يرفع تعتيم 0.6 إلى **0.936** بقانون
`1-(1-a)³`، ويثلّث الحشو، ويجعل كل سطر صندوقاً بعرض مختلف.

**كل مُنزلقات «التعتيم» تقيس alpha: 0% شفاف و100% معتم** — في قسم الترجمة
وقسم الشبكة على السواء، بلا استثناء ولا قلب في أي مسار (البند #54). لا تُسمِّها
«شفافية»؛ الكلمة تصف العكس، وقد أُزيلت من الواجهة كلها. **ولا تناقض بين
القسمين** — قِيس الاتجاه في الاثنين فوُجد متطابقاً.

**لا تحذف `subtitleTrackObserver` ظنّاً أنه زائد** (فُحص في #55 وأُغلق): مستمع
`loadedmetadata` على مستوى `document` **لا يلتقط** فيديو حُمِّل وهو منفصل عن الـ
DOM ثم أُضيف، ولا فيديو مُحمَّل نُقل داخل الـ DOM — أحداث العنصر المنفصل لا تصعد،
والنقل لا يُعيد إطلاق الحدث. المراقب وحده يغطي الحالتين، وفيهما `readyState: 4`
فالمسارات جاهزة فعلاً. نطاقه `body` هو الحل، لا حذفه.

**فخّ تشخيصي: «لون الخلفية» ≠ «لون النافذة» في يوتيوب** (حسمه المالك 2026-07-29).
ليوتيوب إعدادان منفصلان لترجمته: **background color** وهو صندوق النص، و**window
color** وهو اللوح خلف نافذة الترجمة كلها. الإضافة كانت تتحكم بالأول **فقط**، فمن
ضبط لون نافذة رأى صندوقنا داخل نافذته: لوحاً عريضاً بلون لم تختره الإضافة **يبقى
بعد تعطيلها** ويبدو عطباً منّا. العلامة الفارقة: **الصندوق أعرض من النص بكثير**.
عولج في #52 ثم #53، والحالة النهائية موصوفة أعلاه.

**كونسول الصفحة نظيف — لا تُعِد التحقيق** (تحقّق المالك 2026-07-29 بتعطيل كل
الإضافات الأخرى): ملفنا اسمه `content.js` لا `isolated.js`، فأي خطأ يشير إلى
`isolated.js` أو `debounce is not defined` أو `Could not establish connection` أو
`message port closed` **ليس منّا** بل من إضافة أخرى.

**قواعد قائمة من `CLAUDE.md`:** `textContent` لا `innerHTML` مع أي قيمة قادمة من
التخزين · `wheel.actions` هو مصدر الحقيقة للمربعات و`map` مشتقّة منه ·
`ensureZonesDefaults` **قارئة فقط** ولا تكتب إطلاقاً.

---

## 6. بروتوكول العمل

1. **مهمة واحدة في كل دورة** — لا تجمع بندين إلا إذا طلب المستخدم ذلك صراحةً.
2. **أقل عدد أسطر** يحقّق الغرض.
3. **اختبار آلي حيثما أمكن** تحت `tools/`، ويجب أن **يفشل على الكود السابق**.
   وثِّق كم تأكيداً يفشل: #26 يفشل بـ16، و#47 يفشل بـ13.
4. **كومِت مستقل برقم البند**: `[12] وصف بالعربي، فعل ماضٍ، أقل من 60 حرفاً`.
5. **خطوات اختبار يدوي للمستخدم**، مرقّمة وواضحة. ومنصّتا §8 و§9 **مُلزِمتان**
   حسب ما يمسّه البند (قرار المالك 17).
6. **ثم قف وانتظر تأكيده** قبل البند التالي.
7. **فرعنة:** لا تفرّع عملاً من فرع غير مدموج، ولا تترك فرع توثيق معلّقاً بعد
   التحقق منه — يُدمج فوراً (قرار المالك 18).
8. **كل شرط «إن كان كذا» يُقاس على الشجرة والسجل قبل تنفيذه**، وإن خالف الواقع
   فأبلِغ ولا تنفّذ (قرار المالك 16).

> ⚠️ **المستخدم هو من يختبر في المتصفح — أنت لا تستطيع.** لا تدّعِ أنك جرّبت في
> كروم. اكتب له خطوات مرقّمة تشمل مسار النجاح **ومسار عدم الانحدار**، وإن كان
> الاختبار يتطلب افتعال فشل فأعطه سكربت كونسول جاهزاً للصق.
>
> ولا تخترع تعديلاً لبند سليم. إن فحصت شيئاً ووجدته صحيحاً، **قل ذلك** وأضف
> اختبار انحدار بدل تغيير الكود. وإن ناقض القياسُ فرضيةَ المستخدم فقل ذلك
> بالأرقام قبل أن تنفّذ — حدث مراراً وكان الصواب مع القياس.
>
> **وإن لم تُعِد إنتاج عطب بنفسك فقل ذلك صريحاً ولا تُخمّن سبباً.**
>
> **ولا تكتب في التوثيق أن اختباراً «مُعلَّق» وقد نُفِّذ، ولا العكس.** بقي §8
> يقول إن اختبار #56 و#24 لم يُنفَّذ بعد أن نفّذه المالك وأكّده — فجوة تسجيل
> لا فجوة تنفيذ، وقد صُحّحت. سجّل كل تأكيد **بتاريخه** لحظة وقوعه.

---

## 7. البنية والاختبارات

```
content.js        سكربت المحتوى (كل الإطارات، document_start)
background.js     service worker — الهجرة وحدها عند onInstalled، لا شيء غيرها
storage.js        مشترك بين popup.js و options.js و background.js —
                  الكتل المقترنة + safeSyncSet + migrateAll ومكوّناتها
popup.js/html     نافذة الإضافة
options.js/html/css  صفحة الإعدادات
yt_quality_main.js   عالم MAIN لضبط جودة يوتيوب
icons/            16/32/48/128 — يولّدها tools/make-icons.js حتمياً
tools/            أدوات واختبارات — لا تُشحن
                  KNOWN-DEFECTS.md · manual-59-body.html · report-fullscreen-bug.js
AUDIT.md          تقرير الفحص، الترقيم وصل إلى 57 (و58 مُبلَّغ غير مسجَّل بعد)
```

> ⚠️ **`tools/KNOWN-DEFECTS.md` — اقرأه قبل تشغيل أي منصّة قياس** (قرار المالك 20).
> فيه كل فشل **متوقَّع** في `bench-*.mjs` و`repro-*.mjs`، وما ليس فيه فهو انحدار
> أدخلتَه أنت. ومجموعة `test-*.js` **خضراء دائماً** — الأحمر يعني «كسرتَ شيئاً
> الآن»، ومجموعة حمراء دائماً مجموعة ميتة.

**فحص الصحة النحوية:**

```bash
node --check content.js && node --check popup.js && node --check options.js \
  && node --check storage.js && node --check yt_quality_main.js \
  && node --check background.js
node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8'))"
```

**الاختبارات — تعمل بـ node وحده، لا تحتاج شيئاً:**

```bash
node tools/test-migration.js storage.js   # 127
node tools/test-sw-migration.js           #  56
node tools/test-fs-fill-gate.js           #  38  بوابة تمديد الفيديو + كل المخارج
node tools/test-container-choice.js        #  38  اختيار الحاوية + الحتمية + تثبيت #59
node tools/test-fs-report-sync.js         #  29  حارس انحراف مقطع الإبلاغ
node tools/test-top-layer.js              #  54
node tools/test-frame-wake.js             #  42
node tools/test-caption-size.js           #  39
node tools/test-startup-read.js           #  39
node tools/test-grid-appearance.js        #  38
node tools/test-quality-gap.js            #  33
node tools/test-subtitle-observer.js      #  31
node tools/test-caption-window.js         #  30
node tools/test-caption-box.js            #  28
node tools/test-popup-truth.js            #  26
node tools/test-clean-player-captions.js  #  25
node tools/test-preview-hide.js           #  23
node tools/test-precedence.js             #  22
node tools/test-overlay-host.js           #  21
node tools/test-middle-click.js           #  19
node tools/test-opacity-naming.js         #  19
node tools/test-shadow-dom.js             #  19
node tools/test-yt-quality.js             #  19
node tools/test-shorts-gate.js            #  18
node tools/test-reload-once.js            #  17
node tools/test-fullscreen-result.js      #  15
node tools/test-import-failure.js         #  15
node tools/test-safe-set.js               #  15
node tools/test-validate.js               #  15
node tools/test-zone-defaults.js          #  15
node tools/test-boost-frame.js            #  14
node tools/test-native-fs-button.js       #  11
node tools/bench-shadow.js                # قياس أداء بـ node، لا اختبار
```

**المجموع: 949 تأكيداً في 32 ملفاً، كلها ناجحة.** شغّلها كلها قبل أي كومِت.

**أدوات القياس — تحتاج كروم مثبَّتاً، لا تعمل بـ node وحده:**

```bash
node tools/bench-startup.mjs          # تكلفة بدء content.js
node tools/bench-frames.mjs           # الخروج المبكر في الإطارات بلا فيديو
node tools/bench-reload.mjs           # قراءات التخزين لكل حفظ عبر ترتيبات الوصول
node tools/bench-caption-size.mjs     # حجم خط الترجمة عبر مقاسات المشغّل
node tools/bench-yt-quality.mjs       # استطلاع جودة يوتيوب على فيديو يسبقه إعلان
```

> **ملاحظة تشغيلية ليوتيوب الحيّ:** في `--headless=new` تكون
> `document.visibilityState === "hidden"` فلا يبدأ يوتيوب تشغيلاً ولا معاينة.
> العلاج `Page.bringToFront` عبر CDP، ومعه تجاوز الـ User-Agent إلى كروم عادي
> وإلا رفضت صفحات `embed` بخطأ 153. وصفحة يوتيوب الرئيسية لا تعرض شبكتها في
> `headless` أصلاً — استعمل صفحة مشاهدة أو نتائج بحث.

### الأرقام المقيسة — لا تُعاد، قارِن بها

| البند | قبل | بعد |
|---|---|---|
| **#13** بدء `content.js` | `11 قراءة · 12 مفتاحاً · 24.3ms` | `1 قراءة · 4 مفاتيح · 12.0ms` |
| **#13ب** إطارات بلا فيديو | `aljazeera.net` **121 من 122** · `cnn.com` **62 من 63** | 60 إطاراً ⇒ **صفر قراءة**؛ إطار كسول ⇒ صفر ثم قراءة واحدة بعد ظهور الفيديو |
| **#14** قراءات لكل حفظ لكل إطار عامل | 13 في كل الترتيبات | **2** أسوأ حالة · 1 في نفس الدورة. وعلى الجزيرة `13 × 122 = 1586` ⇒ **2** |
| **#19** استطلاع الجودة مع إعلان | 9 نبضات · **ضبط على الإعلان مرتين** | 7 نبضات · **صفر على الإعلان** |
| **#21** نطاق المراقب (20 ثانية) | `documentElement`: 227 استدعاءً (يوتيوب) · 104 (الجزيرة) | `body`: 195 · 56 — أي **32 و48 استدعاءً** ضجيج `<head>` |
| **#49** حجم الترجمة (إعداد 22) | 22px ثابتة على كل مقاس | `531px → 9.9px` · `1280px → 22px` · `1920px → 33px` · سقف 44 |
| **#50** بلا حاوية استعلام | — | **32.66px** لو استُعملت `cqw` وحدها ⇒ الحارس يُرجعها إلى 22px |
| **#53** تعتيم مطلوب 0.6 | **0.936** فعلياً بقانون `1-(1-a)³` | 0.6 — طبقة واحدة |
| **#53** حشو الصندوق | `0.091 / 0.364` مخترعة | **`0 / 0.25`** نسبة يوتيوب المقيسة على 5 مقاسات |
| **#46** اختيار مضيف الـ overlay | — | **+31 نانوثانية** لكل تفاعل مربع = 0.0037ms/ث عند 120 حدث عجلة |
| **#16** اجتياز الظل في مسار العجلة | — | ≈صفر في الحالة الشائعة · **≈217ns** أسوأ حالة |
| **#26** تحديث نسخة لمستخدم مهاجَر | الهجرة لا تقع إلا بفتح الإعدادات | **قراءتان · صفر كتابة**؛ وتثبيت جديد ⇒ صفر كتابة وصفر حذف |
| **#47** المسار الحالي بعد التحصين | — | **صفر استدعاء** على عنصر الـ overlay في المسار العادي وفي ملء الشاشة بحاوية أعلى، بعد عشر دورات |

**يُستثنى عند حزم الإضافة للنشر:** `tools/` · `AUDIT.md` · `AUDIT.html` ·
`HANDOFF.md` · `CLAUDE.md` · `.git/` · `.gitignore`.
(`AUDIT.html` متجاهَل في `.gitignore` أصلاً.)

---

## 8. حالة الاختبار اليدوي — كل الموجة 4 مؤكَّدة

**كل بنود الموجة 4 وكذلك #58 اختبرها المالك في المتصفح وأكّدها قبل الدمج. لا شيء
دُمج بلا تأكيد بشري.** سجل التأكيدات — **وما لم يُنفَّذ يُكتب أنه لم يُنفَّذ**:

| البنود | التأكيد |
|---|---|
| ما قبل #56 | مؤكَّدة أثناء الموجة، بنداً بنداً |
| **#56 و#24** | ✅ **مؤكَّدان من المالك 2026-07-30** — الخطوات التسع أدناه نُفِّذت كلها ونجحت: الـ popup صار يقول الحقيقة على ويكيبيديا والمواقع المحظورة وصفحات الإطارات، والفرق بين «لا فيديو» و«متوقفة» و«محظورة» واضح، ولا تباطؤ على الصفحات الثقيلة، ولا انحدار على يوتيوب. **الفجوة كانت في التسجيل لا في التنفيذ**: §8 السابق بقي يقول «لم تُنفَّذ» بعد أن نُفِّذت، ولذلك يُسجَّل التأكيد هنا بتاريخه |
| **#26** | ✅ **مؤكَّد من المالك 2026-07-30** — تثبيت جديد وتحديث نسخة حقيقيان: الهجرة وقعت على `onInstalled` بـ `update` **بلا فتح صفحة الإعدادات إطلاقاً**، والبصمة لم تتغيّر في التحديث التالي، والتثبيت الجديد صفر كتابة، وصفحة الإعدادات تقرأ ما كتبه الـ SW، والإعدادات استُعيدت كاملة |
| **#47** | ✅ **مؤكَّد من المالك 2026-07-30** — المسار العادي وملء الشاشة بحاوية أعلى بلا أي تغيير (`popover: —` في الحالتين)، والشبكة تظهر فوق `<video>` مكبَّر، والسمة تُزال عند الخروج، و`Esc` والتركيز و popover الصفحة كلها سليمة، ولا انحدار على يوتيوب |
| **#58** | ✅ **مؤكَّد من المالك 2026-07-30** — `d.tube` يملأ الشاشة بلا سواد، و`سمات: 0` و`CSS: false` **داخل ملء الشاشة وبعد الخروج وفي التنقّل الداخلي (SPA)**؛ ويوتيوب و X وتويتش وفيميو **بلا سمة ولا تغيّر**، والمربعات والعجلة والترجمة والتعزيز كما كانت، والشبكة تعود بعد `Esc`. **⚠️ الخطوات 12–15 (لائحة تشغيل داخل العنصر المكبَّر) لم تُنفَّذ بقرار المالك** — فالحدّ المعروف يبقى **«حدّ معروف غير مُختبَر»**، لا «نجح». لا يُسجَّل اختبار لم يقع (درس §8 نفسه: الفجوة كانت في التسجيل لا في التنفيذ، والعكس أسوأ) |

### الخطوات التسع — **مؤكَّدة، أعد استعمالها** لأي بند يمسّ حالة الـ popup

هذه هي منصّة الانحدار لمسار «الـ popup يقول الحقيقة» ومسار التعزيز. أي بند يلمس
`GVZ_STATUS` أو `blockedHosts` أو `findVideoFrameId` يُختبر عليها كاملة:

1. **أغلق كل التبويبات المعنية** (ويكيبيديا، يوتيوب، وأي موقع محظور)، ثم أعد
   تحميل الإضافة من `chrome://extensions`.
2. **ويكيبيديا** — والإضافة مفعّلة عامّاً: افتح الـ popup ← **«الإضافة شغالة لكن
   لا فيديو في هذه الصفحة»**. قبل الإصلاح كان يقول «متوقفة». ومُنزلق التعزيز
   **معطَّل** برسالة «لا يوجد فيديو».
3. **صفحة مشغّلها في iframe** (خبر فيه فيديو مضمَّن، أو صفحة فيها مشغّل يوتيوب
   مدمج): افتح الـ popup ← **«الإضافة شغالة على هذه الصفحة»**، والمُنزلق
   **مفعَّل**، وحرّكه ← يعمل على الفيديو داخل الإطار.
4. **موقع محظور بلا فيديو:** احظر ويكيبيديا من الـ popup، أعد تحميل الصفحة، افتح
   الـ popup ← **«الإضافة محظورة على هذا الموقع»** لا «متوقفة». أزل الحظر ← تعود
   «لا فيديو».
5. **موقع محظور فيه فيديو:** احظره ← «محظورة»، والتعزيز يرفض بسبب `blocked`.
6. **الإيقاف العام:** أطفئ «تفعيل الريماب في جميع المواقع» ← «الإضافة متوقفة»
   على كل صفحة، بفيديو أو بدونه. أعد التفعيل ← تعود.
7. **قواعد الموقع وحدها:** أطفئ العام وفعّل قاعدة الموقع ← «تشتغل بقواعد هذا
   الموقع فقط».
8. **يوتيوب — عدم الانحدار:** افتح فيديو ← «شغالة»، والمربعات والعجلة والترجمة
   والجودة والتعزيز كلها كما كانت. وجرّب سطر تعذّر الجودة إن كان مضبوطاً على 4K.
9. **صفحة إخبارية ثقيلة الإعلانات:** افتح الـ popup ← الحالة صحيحة ولا تباطؤ
   (لا إيقاظ للإطارات النائمة). و**صفحة محمية** (`chrome://extensions`) ←
   «هذه الصفحة محمية من المتصفح».

### منصّة اختبار الهجرة — للبند #26 وأي هجرة مخطط لاحقة

الكونسول المستعمل هو **كونسول الـ service worker**: `chrome://extensions` ←
بطاقة الإضافة ← رابط **service worker**. فتحه **لا يشغّل أي هجرة** — الهجرة لا
تقع إلا على `onInstalled`. **ولا تفتح نافذة الإضافة أثناء الاختبار**: هي تشغّل
هجرة قواعد المواقع وحدها، فتُلبس النتيجة. أما `wheel.actions` فلا يستطيع كتابتها
إلا الـ SW أو صفحة الإعدادات — وهذا ما يجعل ظهورها **دليلاً قاطعاً**.

```js
// (أ) ازرع بيانات قديمة
{
  const all = await chrome.storage.sync.get(null);
  const shards = Object.keys(all).filter(k => k.startsWith("sp:"));
  if (shards.length) await chrome.storage.sync.remove(shards);
  await chrome.storage.sync.set({
    siteProfiles: { "twitch.tv": { enabled: true, mappings: [{ from: "Mouse2", to: "ACTION:TOGGLE_PLAY" }] } },
    settings: {
      blockedHosts: [],
      zones: { enabled: true, fullscreenOnly: false, gridCoverage: "player",
        wheel: { map: {
          "2": { up: ["ACTION:VOLUME:+4"], down: ["ACTION:VOLUME:-4"] },
          "8": { up: ["ACTION:SEEK:+5"],  down: ["ACTION:SEEK:-5"] }
        } } }
    }
  });
  const d = await chrome.storage.sync.get(null);
  console.log("قبل:", { siteProfiles: !!d.siteProfiles,
    شظايا: Object.keys(d).filter(k => k.startsWith("sp:")).length,
    actions: d.settings?.zones?.wheel?.actions === undefined ? "غائبة ✅" : "موجودة ❌" });
}
```

ثم غيّر `"version"` في `manifest.json` (مثلاً `2.11.1`) و**↻ Reload** من
`chrome://extensions` — تغيّر النسخة هو ما يجعل كروم يُطلق `onInstalled` بـ
`reason: "update"`. **ولا تفتح صفحة الإعدادات.** ثم:

```js
// (ب) تحقّق
{
  const d = await chrome.storage.sync.get(null);
  const a = d.settings?.zones?.wheel?.actions;
  const fmt = z => a?.[z]?.map(x => `${x.type}${x.value ?? ""}@${x.key}`).join(" · ");
  console.log("بعد:", {
    "شظية twitch": !!d["sp:twitch.tv"],
    "siteProfiles حُذف": !("siteProfiles" in d),
    "عدد المربعات": a ? Object.keys(a).length : 0,
    "المربع 2": fmt("2"), "المربع 8": fmt("8")
  });
  console.log("بصمة:", JSON.stringify(d).length, "حرفاً · مفاتيح:", Object.keys(d).sort().join());
}
```

المتوقّع: `شظية twitch: true` · `siteProfiles حُذف: true` · `عدد المربعات: 9` ·
`المربع 2: volume+4@up · volume-4@down` · `المربع 8: seek+5@up · seek-5@down`.
وسطر السجل في كونسول الـ SW: `[VIDEO-ZONES] هجرة المخطط update {...}`.
**بصمة ثابتة عبر تحديث ثانٍ = لا تكرار عمل.** وأعد النسخة بعد الانتهاء.

---

## 9. منصّة اختبار Shadow DOM — مؤكَّدة، أعد استعمالها

**البنود 16 و22 و46 و47 كلها مؤكَّدة من المستخدم في المتصفح.** هذه المنصّة تبقى
هنا لأن أي بند يمسّ اكتشاف الفيديو أو الـ overlay يجب أن يُختبر عليها. **مصدر
الفيديو محلي بالكامل — لوحة رسم متحركة تُبَث كـ `MediaStream` — فلا يرثّ برابط
شبكة ميت.** الصق في كونسول أي صفحة:

```js
class VZTestPlayer extends HTMLElement {
  connectedCallback() {
    const sr = this.attachShadow({ mode: "open" });
    sr.innerHTML = `<video controls autoplay muted playsinline
        style="width:640px;height:360px;background:#333"></video>
      <input placeholder="اكتب هنا لاختبار البند 22" style="width:100%;padding:8px;font-size:16px">`;
    const c = Object.assign(document.createElement("canvas"), { width: 640, height: 360 });
    const g = c.getContext("2d");
    let i = 0;
    this.__t = setInterval(() => {
      g.fillStyle = `hsl(${i * 4 % 360},55%,28%)`; g.fillRect(0, 0, 640, 360);
      g.fillStyle = "#fff"; g.font = "700 64px sans-serif"; g.fillText(String(i++), 40, 210);
    }, 100);
    sr.querySelector("video").srcObject = c.captureStream(10);
  }
  disconnectedCallback() { clearInterval(this.__t); }
}
customElements.define("vz-test-player", VZTestPlayer);
const el = document.createElement("vz-test-player");
el.style.cssText = "position:fixed;top:8%;left:8%;z-index:2147483000;background:#000;border:4px solid #22c55e";
document.body.appendChild(el);
```

1. أعد تحميل الإضافة من `chrome://extensions` أولاً.
2. مرّر عجلة الماوس فوق الفيديو داخل الإطار الأخضر ← الشبكة تظهر والأمر ينفَّذ.
3. جرّب النقر ومفاتيح الاختصار فوقه ← تعمل.
4. **البند 22:** انقر في حقل الإدخال داخل المكوّن واكتب أحرفاً وأسهماً ←
   **يجب ألا ينفَّذ أي أمر** ولا يتغيّر الفيديو.
5. انقر خارج الحقل ثم اضغط نفس المفاتيح فوق الفيديو ← تعمل.
6. **البند 46 — دورة ملء شاشة كاملة:** اضغط زر ملء الشاشة في أزرار الفيديو
   الأصلية، اخرج بـ `Esc`، ثم مرّر العجلة ← **الشبكة تظهر**. تأكّد بهذا السطر:

```js
(()=>{const seek=r=>{const h=r.querySelector('.vzWrap');if(h)return h;for(const e of r.querySelectorAll('*'))if(e.shadowRoot){const d=seek(e.shadowRoot);if(d)return d;}return null;};const w=seek(document);console.log('أب:',w?(w.parentNode.host?'#shadow-root في '+w.parentNode.host.tagName:w.parentNode.tagName):'غير موجود','| popover:',w?.getAttribute('popover')??'—','| مفتوح:',w?w.matches(':popover-open'):null,'| صندوق:',w?w.getClientRects().length:0)})()
```

   المتوقّع داخل المكوّن `#shadow-root في VZ-TEST-PLAYER | popover: — | صندوق: 1`،
   وعلى يوتيوب `BODY | popover: — | صندوق: 1`.
7. **البند 47 — أثناء ملء الشاشة نفسه:** اضغط زر ملء الشاشة في أزرار الفيديو
   الأصلية (يكبّر `<video>` نفسه)، و**أنت داخله** مرّر العجلة ← **الشبكة تظهر
   الآن**. المسطرة أثناءه: `popover: manual · مفتوح: true`. وبعد `Esc`:
   `popover: — · مفتوح: false`. (قبل #47 لم تكن تظهر إطلاقاً في هذه الحالة.)
8. **عدم الانحدار:** افتح يوتيوب، وكرّر دورة ملء شاشة كاملة هناك أيضاً —
   ويجب أن يبقى `popover: —` في كل مراحلها، فيوتيوب يكبّر `#movie_player` لا الفيديو.
9. للإزالة: `el.remove()`

> ⚠️ المصدر بثّ حيّ ⇒ `duration === Infinity`، فأوامر `SEEK` لا تعمل عليه
> (البند #34 لا انحدار). اربط أمر **صوت** أو **تشغيل/إيقاف** للتحقق من التنفيذ.
>
> ⚠️ وفي هذه المنصّة الفيديو **ابن مباشر لجذر الظل** (`parentElement === null`)،
> فأمر ملء الشاشة من الإضافة يسقط إلى `<video>` نفسه في `pickFullscreenContainer`.
> هذا هو الطريق الأقصر لإعادة إنتاج **#58** ولاختبار #47 معاً.

---

## 10. إعدادات git المضبوطة

```
core.editor      = true      # لا يُفتح vi عند دمج أو كومِت بلا -m
commit.cleanup   = strip     # تُجرَّد أسطر # من الرسالة
```

`core.editor=true` ضُبط بعد أن علق المستخدم في شاشة `vi` أثناء دمج `--no-ff`.
و`commit.cleanup=strip` ضُبط لأن `--no-edit` مع محرر صوري كان يُدخل أسطر التعليق
داخل نصّ رسالة الدمج. **استخدم `-m` و`--no-edit` دائماً.**

**قواعد المستودع المُلزِمة:** لا كومِت بلا طلب صريح · `git add -A` ثم `commit` ثم
`push` في نفس الخطوة · ممنوع بلا إذن صريح: `push --force` و`reset --hard` و
`rebase` و`commit --amend` على كومِت مرفوع وحذف أي فرع · لا تعديل `.gitignore`
ولا `.claude/settings.json` إلا بطلب صريح.

---

## 11. المتبقي — بالترتيب

### أولاً: #58 — عطب ملء الشاشة
انظر §0. **أولويته فوق الموجة 5 كلها** بقرار المالك. غير مشخَّص بعد.

### ثانياً: الموجة 5 — تنظيف وتوثيق

| # | البند | الملف |
|---|---|---|
| **57** | تعميم `migrateAll` في مسار الاستيراد مع تعميم نصّ رسالة الخطأ | `options.js` |
| 27 | حذف `tryEnableYouTubeCC` — كود ميت 100% | `content.js` |
| 28 | حذف فرع `else if (track.mode === "showing")` الفارغ | `content.js` |
| 29 | حذف كتلة `chrome.tabs.query` المعطّلة بالتعليق | `content.js` |
| 30 | توحيد `getVideoUnderPointer` و`getVideoUnderPointerStrict` | `content.js` |
| 32 | استبدال `innerHTML` بـ `createElement` + `textContent` | `popup.js` · `options.js` |
| 34 | `seek` في البث المباشر عبر `video.seekable.end(0)` | `content.js` |
| 35 | فكّ الكتم مفصولاً عن حساب دلتا الصوت | `content.js` |
| 36 | `.catch` على `saveBlockedSiteState().then(...)` | `popup.js` |
| 38 | متفرقات: overlay بلا داعٍ · مرجع قوي لفيديو منفصل · حقن `yt_quality_main.js` عند التفعيل اليدوي · **توحيد اسم المنتج** | متعدّد |
| — | تحديث `README.md` و`CLAUDE.md`: Sound Booster و Subtitles و`yt_quality_main.js` و`background.js` ورسائل التعزيز — كلها غائبة عن التوثيق | توثيق |

> `AUDIT.md` هو المرجع. ترقيمه وصل إلى **57**، والبنود 43–57 أُضيفت أثناء العمل
> ولم تكن في الفحص الأول. **#58 مُبلَّغ من المالك ولم يُسجَّل في `AUDIT.md` بعد** —
> يُسجَّل مع تشخيصه. وأي بند جديد بعده يأخذ **59** فما بعد — **لا تُعِد ترقيم أي
> بند سابق أبداً.**
